# Logseq — Đối chiếu song ngữ Anh ↔ Việt

Tổng số mục: **1743** (Anh) / **1743** (Việt đã dịch)

Định dạng: `English text (bản dịch tiếng Việt)`

Các mục chưa được dịch sẽ chỉ hiển thị tiếng Anh.

---

## `:account/`

- Authentication (Xác thực)
- With a Logseq account, you can access cloud-based services like Logseq Sync and alpha/beta features. (Với tài khoản Logseq, bạn có thể truy cập các dịch vụ đám mây như Logseq Sync và các tính năng alpha/beta.)
- Billing (Thanh toán)
- Pro plan expired on: {1} (Gói Pro đã hết hạn vào: {1})
- Pro plan expires on: {1} (Gói Pro sẽ hết hạn vào: {1})
- Next billing date: {1} (Ngày thanh toán tiếp theo: {1})
- Access to core Logseq features (Truy cập các tính năng cốt lõi của Logseq)
- Current plan (Gói hiện tại)
- Delete Account (Xoá tài khoản)
- Discover the power of {1} (Khám phá sức mạnh của {1})
- Early access to alpha/beta features (Truy cập sớm các tính năng alpha/beta)
- First name (Tên)
- 1 synced graph (up to 50MB, notes only) (1 đồ thị đồng bộ (tối đa 50MB, chỉ ghi chú))
- Last name (Họ)
- Manage plan (Quản lý gói)
- month (tháng)
- No asset syncing (Không đồng bộ tệp đính kèm)
- Open invoices (Xem hoá đơn)
- Free (Miễn phí)
- Get started with basic syncing (Bắt đầu với đồng bộ cơ bản)
- Pro (Pro)
- Unlock advanced syncing and more (Mở khoá đồng bộ nâng cao và nhiều hơn nữa)
- 10 synced graphs (up to 5GB each) (10 đồ thị đồng bộ (tối đa 5GB mỗi đồ thị))
- Profile (Hồ sơ)
- exceptional status when refresh-token (Trạng thái bất thường khi làm mới token)
- Reset password (Đặt lại mật khẩu)
- Sign up (Đăng ký)
- Sync assets up to 100MB per file (Đồng bộ tệp đính kèm tối đa 100MB mỗi tệp)
- Synced (Đã đồng bộ)
- Unlimited unsynced graphs (Đồ thị không đồng bộ không giới hạn)
- Upcoming cloud-based features, including Logseq Publish (Các tính năng đám mây sắp tới, bao gồm Logseq Publish)
- Upgrade plan (Nâng cấp gói)
- Username (Tên người dùng)

## `:asset/`

- Acceptable file extensions (Phần mở rộng tệp được chấp nhận)
- Add assets (Thêm tệp đính kèm)
- Add directory (Thêm thư mục)
- Alias name of "{1}" already exists! (Tên bí danh "{1}" đã tồn tại!)
- Alias directories (Thư mục bí danh)
- Directory path: (Đường dẫn thư mục:)
- What's the alias name of this selected directory? (Tên bí danh của thư mục đã chọn là gì?)
- Alias name: (Tên bí danh:)
- e.g. Books (ví dụ: Sách)
- Align (Căn chỉnh)
- Align center (Căn giữa)
- Align left (Căn trái)
- Align right (Căn phải)
- Asset exists already, title: {1}, node reference: [[{2}]] (Tệp đính kèm đã tồn tại, tiêu đề: {1}, tham chiếu: [[{2}]])
- Can't embed parent block as its own property (Không thể nhúng khối cha làm thuộc tính của chính nó)
- Are you sure you want to delete this image? (Bạn có chắc chắn muốn xoá hình ảnh này?)
- Copy image (Sao chép hình ảnh)
- Copy image is not supported for {1} files (Không hỗ trợ sao chép hình ảnh cho tệp {1})
- Creating a local asset from an external one. PDF annotations require a local asset to work properly. (Đang tạo bản sao cục bộ từ tệp bên ngoài. Chú thích PDF cần tệp cục bộ để hoạt động đúng.)
- Create asset (Tạo tệp đính kèm)
- Delete image (Xoá hình ảnh)
- Downloading (Đang tải xuống)
- Drag 'n' drop files here, or click to select files (Kéo thả tệp vào đây hoặc nhấn để chọn tệp)
- Edit asset (Sửa tệp đính kèm)
- Asset external URL: (URL bên ngoài của tệp:)
- e.g. mp3 (ví dụ: mp3)
- Maximize image (Phóng to hình ảnh)
- Open image in browser (Mở hình ảnh trong trình duyệt)
- Remove the file too (notice it can't be restored) (Xoá luôn cả tệp (lưu ý: không thể khôi phục))
- Asset ref block (Khối tham chiếu tệp đính kèm)
- Select Asset File (Chọn tệp đính kèm)
- Select from disk (Chọn từ ổ đĩa)
- Selected directories: (Thư mục đã chọn:)
- Show file in folder (Hiển thị tệp trong thư mục)
- Asset size shouldn't be larger than 100M (Kích thước tệp đính kèm không được lớn hơn 100M)
- Syncing (Đang đồng bộ)
- Asset title: (Tiêu đề tệp đính kèm:)
- {1} asset... ({1} tệp đính kèm...)
- Uploading (Đang tải lên)

## `:block/`

- Click to fix query: {1} (Nhấn để sửa truy vấn: {1})
- Copy block ref (Sao chép tham chiếu khối)
- Copy block URL (Sao chép URL khối)
- Created: {1} (Đã tạo: {1})
- '#+BEGIN_EXPORT latex' is deprecated. Use '/Math block' command instead. ('#+BEGIN_EXPORT latex' đã lỗi thời. Hãy dùng lệnh '/Math block' thay thế.)
- #+BEGIN_QUERY is deprecated. Use '/Advanced Query' command instead. (#+BEGIN_QUERY đã lỗi thời. Hãy dùng lệnh '/Advanced Query' thay thế.)
- #+BEGIN_QUOTE is deprecated. Use '/Quote' command instead. (#+BEGIN_QUOTE đã lỗi thời. Hãy dùng lệnh '/Quote' thay thế.)
- Empty URL (URL trống)
- Excalidraw is no longer supported by default, we plan to support it through plugins. (Excalidraw không còn được hỗ trợ mặc định, chúng tôi dự định hỗ trợ thông qua plugin.)
- An unexpected error occurred during block extraction. (Đã xảy ra lỗi không mong muốn khi trích xuất khối.)
- Hide query (Ẩn truy vấn)
- Invalid hiccup (Hiccup không hợp lệ)
- Invalid link (Liên kết không hợp lệ)
- Invalid URL (URL không hợp lệ)
- Large block will not be editable or searchable to not slow down the app, please use another editor to edit this block. (Khối lớn sẽ không thể chỉnh sửa hoặc tìm kiếm để không làm chậm ứng dụng, vui lòng dùng trình soạn thảo khác để sửa khối này.)
- Last edited: {1} (Sửa lần cuối: {1})
- No block found (Không tìm thấy khối)
- Open block references (Mở các tham chiếu của khối)
- Practice (Luyện tập)
- Practice cards (Luyện tập với thẻ)
- Block ref nesting is too deep (Tham chiếu khối lồng nhau quá sâu)
- Remove tag (Xoá thẻ)
- Remove this tag (Xoá thẻ này)
- Block Render Error: (Lỗi hiển thị khối:)
- Retry plugin renderer (Thử lại plugin renderer)
- Set query (Đặt truy vấn)
- Set query: (Đặt truy vấn:)
- Sort order (Thứ tự sắp xếp)
- Status history (Lịch sử trạng thái)
- Switch to outline view (Chuyển sang chế độ phác thảo)
- Switch to plugin renderer (Chuyển sang plugin renderer)
- Untitled query (Truy vấn không tên)

## `:block.macro/`

- {{embed}} is deprecated. Use '/Node embed' command instead. ({{embed}} đã lỗi thời. Hãy dùng lệnh '/Node embed' thay thế.)
- {{namespace}} is deprecated. Use the {1} feature instead. ({{namespace}} đã lỗi thời. Hãy dùng tính năng {1} thay thế.)
- {{query}} is deprecated. Use '/Query' command instead. ({{query}} đã lỗi thời. Hãy dùng lệnh '/Query' thay thế.)
- Unsupported macro name: {1} (Tên macro không được hỗ trợ: {1})

## `:block.reaction/`

- Please pick an emoji reaction. (Vui lòng chọn một biểu tượng cảm xúc.)
- Unsupported reaction emoji. (Biểu tượng cảm xúc không được hỗ trợ.)

## `:bug-report/`

- Can you help us out by submitting a bug report? We'll get it sorted out as soon as we can. (Bạn có thể giúp chúng tôi bằng cách gửi báo cáo lỗi không? Chúng tôi sẽ xử lý sớm nhất có thể.)
- Bug report (Báo cáo lỗi)

## `:bug-report.clipboard/`

- Inspect and collect clipboard data (Kiểm tra và thu thập dữ liệu clipboard)
- Clipboard helper (Trợ lý clipboard)
- You can use these handy tools to give us additional information. (Bạn có thể dùng các công cụ này để cung cấp thêm thông tin cho chúng tôi.)
- Is the bug you encountered related to these features? (Lỗi bạn gặp có liên quan đến các tính năng này không?)

## `:bug-report.inspector/`

- Go back (Quay lại)
- Here is the data read from clipboard. (Đây là dữ liệu đã đọc từ clipboard.)
- Copied to clipboard! (Đã sao chép vào clipboard!)
- Copy the result (Sao chép kết quả)
- If this is okay to share, click the copy button. (Nếu nội dung này không có thông tin nhạy cảm, hãy nhấn nút sao chép.)
- Create an issue (Tạo issue)
- Now you can report the result pasted to your clipboard. Please paste the result in the 'Additional Context' section and state where you copied the original content from. Thanks! (Bây giờ bạn có thể báo cáo kết quả đã dán vào clipboard. Vui lòng dán kết quả vào mục 'Additional Context' và cho biết bạn đã sao chép từ đâu. Cảm ơn!)
- Press {1} to inspect your clipboard data
or click here to paste if you are using the mobile version (Nhấn {1} để kiểm tra dữ liệu clipboard
hoặc nhấn vào đây để dán nếu bạn đang dùng phiên bản di động)
- Long press here to paste if you are on mobile (Nhấn giữ ở đây để dán nếu bạn đang dùng di động)
- Something wrong? No problem, click to go back to the previous step. (Có gì sai sao? Không sao, hãy nhấn để quay lại bước trước.)
- Clipboard data inspector (Trình kiểm tra dữ liệu clipboard)

## `:bug-report.issue/`

- Help Make Logseq Better! (Giúp Logseq tốt hơn!)
- Submit a bug report (Gửi báo cáo lỗi)
- If there are no tools available for you to gather additional information, please report the bug directly. (Nếu không có công cụ nào để thu thập thông tin bổ sung, vui lòng báo cáo lỗi trực tiếp.)
- Report issue (Báo cáo lỗi)
- Or... (Hoặc...)

## `:class/`

- Add tag property (Thêm thuộc tính cho thẻ)
- Tag properties are inherited by all nodes using the tag. For example, each #Task node inherits 'Status' and 'Priority'. (Thuộc tính của thẻ được kế thừa bởi tất cả các nút sử dụng thẻ đó. Ví dụ, mỗi nút #Task kế thừa 'Status' và 'Priority'.)
- Tagged nodes (Các nút được gắn thẻ)

## `:class.built-in/`

- Asset (Tệp đính kèm)
- Card (Thẻ ghi nhớ)
- Cards (Thẻ ghi nhớ)
- Code (Mã)
- Journal (Nhật ký)
- Math (Toán học)
- Page (Trang)
- PDF Annotation (Chú thích PDF)
- Property (Thuộc tính)
- Query (Truy vấn)
- Quote (Trích dẫn)
- Root Tag (Thẻ gốc)
- Tag (Thẻ)
- Task (Tác vụ)
- Template (Mẫu)
- Whiteboard (Bảng trắng)

## `:class.validation/`

- Can't change the extends of a built-in tag. (Không thể thay đổi extends của thẻ tích hợp sẵn.)
- Can't add tag on built-in "{1}". (Không thể thêm thẻ trên "{1}" tích hợp sẵn.)
- Can't remove private tags: {1}. (Không thể xoá thẻ riêng tư: {1}.)
- Can't remove tag with built-in #{1}. (Không thể xoá thẻ với #{1} tích hợp sẵn.)
- Can't remove tag on built-in "{1}". (Không thể xoá thẻ trên "{1}" tích hợp sẵn.)
- Can't set tag with built-in #{1}. (Không thể đặt thẻ với #{1} tích hợp sẵn.)
- Another tag named "{1}" already exists. (Đã tồn tại thẻ khác có tên "{1}".)
- Tag extends cycle. (Vòng lặp extends của thẻ.)
- Can't extend this page since either it is not a tag or is extending from a page that is not a tag. (Không thể extends trang này vì nó không phải thẻ hoặc đang extends từ trang không phải thẻ.)
- Cannot create this tag unless all parents are tags. (Không thể tạo thẻ này trừ khi tất cả các thẻ cha đều là thẻ.)
- Can't set tag with built-in page that isn't a tag "{1}". (Không thể đặt thẻ với trang tích hợp sẵn không phải thẻ "{1}".)

## `:cmdk.action/`

- Apply theme (Áp dụng giao diện)
- Copy ref (Sao chép tham chiếu)
- Create (Tạo)
- Filter (Lọc)
- Open (Mở)
- Open in sidebar (Mở trong thanh bên)
- Search (Tìm kiếm)
- Trigger (Kích hoạt)

## `:cmdk.create/`

- Configure tag (Cấu hình thẻ)
- Create page (Tạo trang)
- Create tag (Tạo thẻ)

## `:cmdk.error/`

- No link found in this block's content. (Không tìm thấy liên kết trong nội dung khối này.)
- No link found in this page's properties. (Không tìm thấy liên kết trong thuộc tính của trang này.)
- No link for this search item. (Không có liên kết cho mục tìm kiếm này.)

## `:cmdk.filter/`

- Add filter to search (Thêm bộ lọc vào tìm kiếm)
- Search only codes (Chỉ tìm mã code)
- Search only commands (Chỉ tìm lệnh)
- Search only current page (Chỉ tìm trong trang hiện tại)
- Search only files (Chỉ tìm tệp)
- Search only nodes (Chỉ tìm các nút)
- Search only: (Chỉ tìm:)
- Search only themes (Chỉ tìm giao diện)

## `:cmdk.group/`

- Code (Mã code)
- Commands (Lệnh)
- Create (Tạo)
- Current page (Trang hiện tại)
- Files (Tệp)
- Filters (Bộ lọc)
- Nodes (Các nút)
- Recently updated (Vừa cập nhật)
- Themes (Giao diện)

## `:cmdk.info/`

- Configure #{1} (Cấu hình #{1})
- Create page called '{1}' (Tạo trang tên '{1}')
- Create tag called '{1}' (Tạo thẻ tên '{1}')

## `:cmdk.input/`

- Add graph filter (Thêm bộ lọc đồ thị)
- What are you looking for? (Bạn đang tìm gì?)
- Move blocks to (Di chuyển các khối đến)
- Type a page name to create (Nhập tên trang để tạo)

## `:cmdk.tip/`

- Press {1} to clear search filter (Nhấn {1} để xoá bộ lọc tìm kiếm)
- Press {1} to filter search results (Nhấn {1} để lọc kết quả tìm kiếm)
- Tip: (Mẹo:)
- Press {1} to open search in the sidebar (Nhấn {1} để mở tìm kiếm trong thanh bên)

## `:collaboration/`

- Email address (Địa chỉ email)
- Invite (Mời)
- Members: (Thành viên:)
- Remove access (Xoá quyền truy cập)
- Failed to remove member. (Không thể xoá thành viên.)

## `:color/`

- Blue (Xanh dương)
- Crimson (Đỏ thẫm)
- Cyan (Xanh lơ)
- Grass (Xanh cỏ)
- Gray (Xám)
- Green (Xanh lá)
- Indigo (Chàm)
- Orange (Cam)
- Pink (Hồng)
- Plum (Mận)
- Purple (Tím)
- Red (Đỏ)
- Teal (Xanh mòng két)
- Tomato (Cà chua)
- Violet (Tím violet)
- Yellow (Vàng)

## `:command.auto-complete/`

- Auto-complete: Choose selected item (Tự động hoàn thành: Chọn mục đã chọn)
- Auto-complete: Cmd + Enter to choose selected item (Tự động hoàn thành: Cmd + Enter để chọn mục)
- Auto-complete: Select next item (Tự động hoàn thành: Chọn mục tiếp theo)
- Auto-complete: Select previous item (Tự động hoàn thành: Chọn mục trước)
- Auto-complete: Open selected item in sidebar (Tự động hoàn thành: Mở mục đã chọn trong thanh bên)

## `:command.command-palette/`

- Search commands (Tìm kiếm lệnh)

## `:command.editor/`

- Add property (Thêm thuộc tính)
- Add task deadline to selected block (Thêm hạn chót cho khối đã chọn)
- Add icon (Thêm biểu tượng)
- Add task priority to selected block (Thêm độ ưu tiên cho khối đã chọn)
- Add task status to selected block (Thêm trạng thái cho khối đã chọn)
- Add reaction (Thêm phản hồi)
- Backspace / Delete backwards (Backspace / Xoá lùi)
- Delete a word backwards (Xoá một từ về phía sau)
- Move cursor backward a word (Di chuyển con trỏ lùi một từ)
- Move cursor to the beginning of a block (Di chuyển con trỏ về đầu khối)
- Bold (In đậm)
- Delete entire block content (Xoá toàn bộ nội dung khối)
- Collapse (Thu gọn)
- Copy (copies either selection, or block reference) (Sao chép (sao chép phần đã chọn hoặc tham chiếu khối))
- Copy a block embed pointing to the current block (Sao chép nhúng khối trỏ tới khối hiện tại)
- Copy page url (Sao chép URL trang)
- Copy selections as text (Sao chép phần đã chọn dưới dạng văn bản)
- Cut (Cắt)
- Rotate the TODO state (Chuyển trạng thái TODO)
- Delete / Delete forwards (Xoá / Xoá tới)
- Delete selected blocks (Xoá các khối đã chọn)
- Move cursor down / Select down (Di chuyển con trỏ xuống / Chọn xuống)
- Move cursor to the end of a block (Di chuyển con trỏ về cuối khối)
- Escape editing (Thoát khỏi chế độ chỉnh sửa)
- Expand (Mở rộng)
- Follow link under cursor (Theo liên kết dưới con trỏ)
- Delete a word forwards (Xoá một từ về phía trước)
- Move cursor forward a word (Di chuyển con trỏ tới một từ)
- Highlight (Tô sáng)
- Indent block (Tăng thụt lề khối)
- HTML Link (Liên kết HTML)
- Insert youtube timestamp (Chèn dấu thời gian YouTube)
- Italics (In nghiêng)
- Jump to a property key or value (Nhảy tới khoá hoặc giá trị thuộc tính)
- Delete line after cursor position (Xoá dòng sau con trỏ)
- Delete line before cursor position (Xoá dòng trước con trỏ)
- Move cursor left / Open selected block at beginning (Di chuyển con trỏ trái / Mở khối đã chọn ở đầu)
- Move block down (Di chuyển khối xuống)
- Move block up (Di chuyển khối lên)
- Move blocks to (Di chuyển các khối đến)
- Create new block (Tạo khối mới)
- New line in current block (Dòng mới trong khối hiện tại)
- Edit selected block (Sửa khối đã chọn)
- Open link in sidebar (Mở liên kết trong thanh bên)
- Open selected block(s) in sidebar (Mở (các) khối đã chọn trong thanh bên)
- Outdent block (Giảm thụt lề khối)
- Paste text into one block at point (Dán văn bản vào một khối tại vị trí con trỏ)
- Quick add (Thêm nhanh)
- Redo (Làm lại)
- Move cursor right / Open selected block at end (Di chuyển con trỏ phải / Mở khối đã chọn ở cuối)
- Select all blocks (Chọn tất cả các khối)
- Select block below (Chọn khối phía dưới)
- Select block above (Chọn khối phía trên)
- Select content below (Chọn nội dung phía dưới)
- Select parent block (Chọn khối cha)
- Select content above (Chọn nội dung phía trên)
- Set tags for selected block(s) (Đặt thẻ cho (các) khối đã chọn)
- Strikethrough (Gạch ngang)
- Toggle expand/collapse (Bật/tắt mở rộng/thu gọn)
- Toggle display hidden properties (Bật/tắt hiển thị thuộc tính ẩn)
- Toggle number list (Bật/tắt danh sách số)
- Toggle open blocks (collapse or expand all blocks) (Bật/tắt mở khối (thu gọn hoặc mở rộng tất cả))
- Undo (Hoàn tác)
- Move cursor up / Select up (Di chuyển con trỏ lên / Chọn lên)
- Zoom in editing block / Forwards otherwise (Phóng to khối đang sửa / Tới)
- Zoom out editing block / Backwards otherwise (Thu nhỏ khối đang sửa / Lùi)

## `:command.go/`

- Go to all graphs (Đến tất cả các đồ thị)
- Go to all pages (Đến tất cả các trang)
- Backwards (Lùi)
- Find text in page (Tìm văn bản trong trang)
- Jump to the next match to your Find bar search (Đến kết quả tìm kiếm tiếp theo)
- Jump to the previous match to your Find bar search (Đến kết quả tìm kiếm trước)
- Toggle flashcards (Bật/tắt thẻ ghi nhớ)
- Forwards (Tiến)
- Go to graph view (Đến chế độ đồ thị)
- Go to home (Đến trang chủ)
- Go to journals (Đến nhật ký)
- Go to keyboard shortcuts (Đến phím tắt bàn phím)
- Go to next journal (Đến nhật ký tiếp theo)
- Go to previous journal (Đến nhật ký trước)
- Search pages and blocks (Tìm kiếm trang và khối)
- Search blocks in page (Tìm kiếm khối trong trang)
- Search themes (Tìm kiếm giao diện)
- Go to tomorrow (Đến ngày mai)

## `:command.graph/`

- Add a graph (Thêm đồ thị)
- Add a DB graph (Thêm đồ thị DB)
- Save the current db to the disk (~/logseq/graphs/your-current-graph) (Lưu db hiện tại vào ổ đĩa (~/logseq/graphs/your-current-graph))
- Export public graph pages as HTML (Xuất các trang công khai dưới dạng HTML)
- Select graph to open (Chọn đồ thị để mở)
- Remove a graph (Xoá một đồ thị)

## `:command.misc/`

- Copy (Sao chép)
- Export block EDN data (Xuất dữ liệu EDN của khối)
- Export graph's tags and properties EDN data (Xuất dữ liệu EDN của thẻ và thuộc tính của đồ thị)
- Export page EDN data (Xuất dữ liệu EDN của trang)
- Import EDN data (Nhập dữ liệu EDN)

## `:command.page/`

- Add to/remove from favorites (Thêm/xoá khỏi mục yêu thích)

## `:command.pdf/`

- PDF: Close current pdf doc (PDF: Đóng tài liệu PDF hiện tại)
- PDF: Search text of current pdf doc (PDF: Tìm văn bản trong tài liệu PDF hiện tại)
- PDF: Next page of current pdf doc (PDF: Trang tiếp theo của tài liệu PDF hiện tại)
- PDF: Previous page of current pdf doc (PDF: Trang trước của tài liệu PDF hiện tại)

## `:command.publish/`

- Open publish dialog for current page (Mở hộp thoại xuất bản cho trang hiện tại)

## `:command.search/`

- Rebuild search index (Xây dựng lại chỉ mục tìm kiếm)

## `:command.shell/`

- Run git command (Chạy lệnh git)

## `:command.sidebar/`

- Clear all in the right sidebar (Xoá tất cả trong thanh bên phải)
- Closes the top item in the right sidebar (Đóng mục trên cùng trong thanh bên phải)
- Open today's page in the right sidebar (Mở trang hôm nay trong thanh bên phải)

## `:command.ui/`

- Clear all notifications (Xoá tất cả thông báo)
- Customize appearance (Tuỳ chỉnh giao diện)
- Go to plugins dashboard (Đến bảng điều khiển plugin)
- Toggle highlight recent blocks (Bật/tắt tô sáng các khối gần đây)
- Install plugin from GitHub release (Cài plugin từ bản phát hành GitHub)
- Install plugins from plugins.edn (Cài plugin từ plugins.edn)
- Select available theme colors (Chọn màu giao diện có sẵn)
- Toggle whether to display brackets (Bật/tắt hiển thị ngoặc vuông)
- Toggle Contents in sidebar (Bật/tắt Mục lục trong thanh bên)
- Toggle document mode (Bật/tắt chế độ tài liệu)
- Toggle help (Bật/tắt trợ giúp)
- Toggle left sidebar (Bật/tắt thanh bên trái)
- Toggle right sidebar (Bật/tắt thanh bên phải)
- Toggle settings (Bật/tắt cài đặt)
- Toggle between dark/light theme (Chuyển đổi giao diện sáng/tối)
- Toggle wide mode (Bật/tắt chế độ rộng)

## `:command.window/`

- Close window (Đóng cửa sổ)

## `:context-menu/`

- Developer tools (Công cụ dành cho nhà phát triển)
- Make a Flashcard (Tạo thẻ ghi nhớ)
- Set icon (Đặt biểu tượng)
- Toggle number list (Bật/tắt danh sách số)

## `:date/`

- {1} is not a valid date. Please try again ({1} không phải là ngày hợp lệ. Vui lòng thử lại)

## `:date.nlp/`

- Last Friday (Thứ Sáu tuần trước)
- Last Monday (Thứ Hai tuần trước)
- Last month (Tháng trước)
- Last Saturday (Thứ Bảy tuần trước)
- Last Sunday (Chủ Nhật tuần trước)
- Last Thursday (Thứ Năm tuần trước)
- Last Tuesday (Thứ Ba tuần trước)
- Last Wednesday (Thứ Tư tuần trước)
- Last week (Tuần trước)
- Last year (Năm trước)
- Next Friday (Thứ Sáu tuần sau)
- Next Monday (Thứ Hai tuần sau)
- Next month (Tháng sau)
- Next Saturday (Thứ Bảy tuần sau)
- Next Sunday (Chủ Nhật tuần sau)
- Next Thursday (Thứ Năm tuần sau)
- Next Tuesday (Thứ Ba tuần sau)
- Next Wednesday (Thứ Tư tuần sau)
- Next week (Tuần sau)
- Next year (Năm sau)
- This Friday (Thứ Sáu tuần này)
- This Monday (Thứ Hai tuần này)
- This month (Tháng này)
- This Saturday (Thứ Bảy tuần này)
- This Sunday (Chủ Nhật tuần này)
- This Thursday (Thứ Năm tuần này)
- This Tuesday (Thứ Ba tuần này)
- This Wednesday (Thứ Tư tuần này)
- This week (Tuần này)
- This year (Năm nay)
- Today (Hôm nay)
- Tomorrow (Ngày mai)
- Yesterday (Hôm qua)

## `:deeplink/`

- Open link failed. Block-id `{1}` doesn't exist in the graph. (Mở liên kết thất bại. Block-id `{1}` không tồn tại trong đồ thị.)
- Open graph failed. Graph `{1}` doesn't exist. (Mở đồ thị thất bại. Đồ thị `{1}` không tồn tại.)
- Open link failed. Page `{1}` doesn't exist in the graph. (Mở liên kết thất bại. Trang `{1}` không tồn tại trong đồ thị.)

## `:editor/`

- Please add some content first. (Vui lòng thêm nội dung trước.)
- Auto heading (Tiêu đề tự động)
- Search for a block (Tìm kiếm khối)
- Click to edit (Nhấn để sửa)
- Choose language (Chọn ngôn ngữ)
- Collapse all (Thu gọn tất cả)
- Cut (Cắt)
- Rotate the TODO state (Chuyển trạng thái TODO)
- Delete selected blocks (Xoá các khối đã chọn)
- to display this tag inline instead of at the end of this node. (để hiển thị thẻ này nội tuyến thay vì ở cuối nút này.)
- Expand all (Mở rộng tất cả)
- Heading {1} (Tiêu đề {1})
- New page (Trang mới)
- New tag (Thẻ mới)
- There's no block selected, please select blocks first. (Không có khối nào được chọn, vui lòng chọn khối trước.)
- Paste (Dán)
- To reference a node, please use `[[]]`. (Để tham chiếu một nút, vui lòng dùng `[[]]`.)
- Remove heading (Xoá tiêu đề)
- Search for a node (Tìm kiếm nút)
- Search for a tag (Tìm kiếm thẻ)
- Search for a template (Tìm kiếm mẫu)
- Template insert error: {1} (Lỗi chèn mẫu: {1})

## `:editor.document-mode/`

- {1} to create new block ({1} để tạo khối mới)
- Document mode (Chế độ tài liệu)
- Click `D` or type {1} to toggle document mode (Nhấn `D` hoặc gõ {1} để bật/tắt chế độ tài liệu)

## `:editor.quick-add/`

- Add to today (Thêm vào hôm nay)
- Quick add (Thêm nhanh)

## `:editor.slash/`

- Advanced Query (Truy vấn nâng cao)
- Create an advanced query block (Tạo khối truy vấn nâng cao)
- Calculator (Máy tính)
- Insert a calculator (Chèn máy tính)
- Cloze (Cloze)
- Code block (Khối mã)
- Insert code block (Chèn khối mã)
- Current time (Thời gian hiện tại)
- Insert current time (Chèn thời gian hiện tại)
- Date picker (Chọn ngày)
- Pick a date and insert here (Chọn ngày và chèn vào đây)
- Embed HTML (Nhúng HTML)
- Embed Twitter tweet (Nhúng tweet Twitter)
- Embed Video URL (Nhúng URL video)
- Embed YouTube timestamp (Nhúng dấu thời gian YouTube)
- ADVANCED (NÂNG CAO)
- BASIC (CƠ BẢN)
- FORMAT (ĐỊNH DẠNG)
- Heading (Tiêu đề)
- LIST TYPE (KIỂU DANH SÁCH)
- PLUGINS (PLUGIN)
- PRIORITY (ĐỘ ƯU TIÊN)
- TASK DATE (NGÀY TÁC VỤ)
- TASK STATUS (TRẠNG THÁI TÁC VỤ)
- TIME & DATE (THỜI GIAN & NGÀY)
- Heading {1} (Tiêu đề {1})
- Image link (Liên kết hình ảnh)
- Create a HTTP link to an image (Tạo liên kết HTTP đến hình ảnh)
- Create a HTTP link (Tạo liên kết HTTP)
- Math block (Khối toán học)
- Create a LaTeX block (Tạo khối LaTeX)
- No priority (Không ưu tiên)
- Node embed (Nhúng nút)
- Embed a node here (Nhúng một nút vào đây)
- Node reference (Tham chiếu nút)
- Create a backlink to a node (a page or a block) (Tạo backlink đến một nút (trang hoặc khối))
- Normal text (Văn bản thường)
- Clear heading and set to normal text (Xoá tiêu đề và đặt thành văn bản thường)
- Number children (Đánh số các khối con)
- Number list (Danh sách số)
- Set priority to {1} (Đặt độ ưu tiên thành {1})
- Priority {1} (Độ ưu tiên {1})
- Query function (Hàm truy vấn)
- Create a query function (Tạo hàm truy vấn)
- Create a quote block (Tạo khối trích dẫn)
- Set status to {1} (Đặt trạng thái thành {1})
- Insert a created template here (Chèn mẫu đã tạo vào đây)
- Insert the date of today (Chèn ngày hôm nay)
- Insert the date of tomorrow (Chèn ngày mai)
- Underline (Gạch dưới)
- Create an underline text decoration (Tạo trang trí văn bản gạch dưới)
- Upload an asset (Tải lên tệp đính kèm)
- Upload file types like image, PDF, DOCX, etc. (Tải lên các loại tệp như hình ảnh, PDF, DOCX, v.v.)
- Insert the date of yesterday (Chèn ngày hôm qua)

## `:electron/`

- About Logseq (Về Logseq)
- Add to dictionary (Thêm vào từ điển)
- Open link failed. Block-id `{1}` doesn't exist in the graph. (Mở liên kết thất bại. Block-id `{1}` không tồn tại trong đồ thị.)
- Cancel (Huỷ)
- Copy Image (Sao chép hình ảnh)
- Are you sure you want to open this link? 
{1} (Bạn có chắc muốn mở liên kết này không? 
{1})
- Failed to open link. Missing graph identifier after `logseq://graph/`. (Không thể mở liên kết. Thiếu mã nhận dạng đồ thị sau `logseq://graph/`.)
- Failed to open link. Cannot match graph identifier `{1}` to any linked graph. (Không thể mở liên kết. Không thể khớp mã đồ thị `{1}` với bất kỳ đồ thị nào.)
- Failed to open link. Cannot match `{1}` to any target. (Không thể mở liên kết. Không thể khớp `{1}` với đích nào.)
- Look up selection (Tra cứu phần đã chọn)
- [Main Exception]
{1}
{2} ([Main Exception]
{1}
{2})
- New Window (Cửa sổ mới)
- Official Documentation (Tài liệu chính thức)
- OK (OK)
- Opening the specified directory failed.
{1} (Không thể mở thư mục đã chỉ định.
{1})
- Save Image (Lưu hình ảnh)
- Save Image As... (Lưu hình ảnh thành...)
- Search with Google (Tìm kiếm bằng Google)
- Unimplemented x-callback-url action: `{1}`. (Hành động x-callback-url chưa được hiện thực: `{1}`.)
- Version {1} (Phiên bản {1})
- Write to the file {1} failed, {2}. (Ghi tệp {1} thất bại, {2}.)
- Write to the file {1} failed, {2}. A backup file was saved to {3}. (Ghi tệp {1} thất bại, {2}. Đã lưu tệp sao lưu vào {3}.)
- Open nightly release (Mở bản nightly)
- Download regular release (Tải bản chính thức)
- This release runs through Rosetta and can be very slow. Download the Apple Silicon (arm64) release first. If it is unavailable for your version, use the nightly release. (Bản phát hành này chạy qua Rosetta và có thể rất chậm. Hãy tải bản Apple Silicon (arm64) trước. Nếu chưa có cho phiên bản của bạn, hãy dùng bản nightly.)
- You are using the Intel (x64) release on Apple Silicon. (Bạn đang dùng bản Intel (x64) trên Apple Silicon.)

## `:encryption/`

- Current password (Mật khẩu hiện tại)
- Enter password (Nhập mật khẩu)
- Enter password again (Nhập lại mật khẩu)
- Enter password for remote graphs (Nhập mật khẩu cho đồ thị từ xa)
- Failed to force resetting password. (Không thể buộc đặt lại mật khẩu.)
- Failed to update password. (Không thể cập nhật mật khẩu.)
- Fetching user rsa-key-pair err: {1} (Lỗi lấy cặp khoá rsa: {1})
- Fetching user rsa-key-pair... (Đang lấy cặp khoá rsa...)
- Force reset password (Buộc đặt lại mật khẩu)
- Force reset password successfully! (Buộc đặt lại mật khẩu thành công!)
- Force resetting password ... (Đang buộc đặt lại mật khẩu...)
- Forgot password? (Quên mật khẩu?)
- If you forget your password, you can force a reset of your encryption password. However, this will make all currently encrypted graph data stored on the server permanently unreadable. After resetting, you'll need to re-upload your graphs from the client. (Nếu bạn quên mật khẩu, có thể buộc đặt lại mật khẩu mã hoá. Tuy nhiên, điều này sẽ làm cho toàn bộ dữ liệu đồ thị mã hoá hiện đang lưu trên máy chủ vĩnh viễn không đọc được. Sau khi đặt lại, bạn sẽ phải tải lên lại đồ thị từ máy khách.)
- Init E2EE encrypt-key-pair (Khởi tạo cặp khoá mã hoá E2EE)
- Init key-pair err: {1} (Lỗi khởi tạo cặp khoá: {1})
- Password not matched (Mật khẩu không khớp)
- Password updated successfully! (Cập nhật mật khẩu thành công!)
- Reset password (Đặt lại mật khẩu)
- Set new password (Đặt mật khẩu mới)
- Set password for remote graphs (Đặt mật khẩu cho đồ thị từ xa)
- Updating password ... (Đang cập nhật mật khẩu...)
- Wrong password (Sai mật khẩu)

## `:export/`

- Backup successful! (Sao lưu thành công!)
- Copied block's data! (Đã sao chép dữ liệu khối!)
- Collecting assets (Đang thu thập tệp đính kèm)
- Copied to clipboard! (Đã sao chép vào clipboard!)
- Copy / Export as.. (Sao chép / Xuất ra dạng...)
- Creating zip (Đang tạo tệp zip)
- DB backup failed, please go to Export and specify a backup folder. (Sao lưu DB thất bại, vui lòng vào Xuất và chỉ định thư mục sao lưu.)
- Export EDN file (Xuất tệp EDN)
- Exports to a .transit file to send to us for debugging. Any sensitive data will be removed in the exported file. (Xuất ra tệp .transit để gửi cho chúng tôi gỡ lỗi. Mọi dữ liệu nhạy cảm sẽ bị xoá khỏi tệp xuất.)
- Export debug transit file (Xuất tệp transit gỡ lỗi)
- Exports to a readable and editable .edn file. Don't rely on this as a primary backup. (Xuất ra tệp .edn dễ đọc và sửa được. Đừng dùng làm bản sao lưu chính.)
- An unexpected error occurred during export. See the javascript console for details. (Đã xảy ra lỗi không mong muốn khi xuất. Xem chi tiết ở console javascript.)
- Exporting (Đang xuất)
- Finalizing (Đang hoàn thiện)
- Text (Văn bản)
- Export graph (Xuất đồ thị)
- Copied graphs's ontology data! (Đã sao chép dữ liệu ontology của đồ thị!)
- dashes (gạch ngang)
- Indentation style: (Kiểu thụt lề:)
- no-indent (không thụt lề)
- spaces (khoảng trắng)
- level <= (cấp độ <=)
- Export as standard Markdown (no block properties) (Xuất dưới dạng Markdown chuẩn (không có thuộc tính khối))
- newline after block (xuống dòng sau khối)
- Graph has not been updated since last export. (Đồ thị không có cập nhật kể từ lần xuất trước.)
- open blocks only (skip collapsed children) (chỉ các khối mở (bỏ qua các khối con đã thu gọn))
- Export page (Xuất trang)
- Copied page's data! (Đã sao chép dữ liệu trang!)
- [[text]] -> text ([[text]] -> text)
- Preparing zip (Đang chuẩn bị tệp zip)
- export preview (xem trước xuất)
- Export public pages (Xuất các trang công khai)
- remove emphasis (xoá nhấn mạnh)
- remove properties (xoá thuộc tính)
- remove #tags (xoá #thẻ)
- Save to file (Lưu vào tệp)
- Export SQLite DB (Xuất SQLite DB)
- Export (Xuất)
- Transparent background (Nền trong suốt)
- Copied view nodes' data! (Đã sao chép dữ liệu các nút trong view!)
- Export both SQLite DB and assets (Xuất cả SQLite DB và tệp đính kèm)
- Export zip failed. (Xuất zip thất bại.)

## `:export.backup/`

- Backup now (Sao lưu ngay)
- Cancel backup (Huỷ sao lưu)
- Hourly backups are enabled for this graph, {1}. (Sao lưu hàng giờ đã bật cho đồ thị này, {1}.)
- Backup folder: (Thư mục sao lưu:)
- Backup will be created every hour. (Bản sao lưu sẽ được tạo mỗi giờ.)
- open backups folder for this graph (mở thư mục sao lưu cho đồ thị này)
- Schedule backup (Lập lịch sao lưu)
- Set backup folder first (Đặt thư mục sao lưu trước)
- Primary way to backup graph's content to a single .sqlite file. (Cách sao lưu chính cho nội dung của đồ thị thành một tệp .sqlite duy nhất.)
- Your browser doesn't support {{The File System Access API}}, please switch to a Chromium-based browser. (Trình duyệt của bạn không hỗ trợ {{The File System Access API}}, hãy chuyển sang trình duyệt dựa trên Chromium.)
- Primary way to backup graph's content and assets to a .zip file. (Cách sao lưu chính cho nội dung và tệp đính kèm của đồ thị thành một tệp .zip.)

## `:file/`

- The file '{1}' has duplicate keys. The key '{2}' is assigned multiple times. (Tệp '{1}' có khoá trùng lặp. Khoá '{2}' được gán nhiều lần.)
- The file 'logseq/config.edn' is invalid. Please reload the app to in order to see the error and fix it. (Tệp 'logseq/config.edn' không hợp lệ. Vui lòng tải lại ứng dụng để xem lỗi và sửa.)
- Format .{1} is not supported. (Định dạng .{1} không được hỗ trợ.)
- File (Tệp)
- Last modified at (Sửa lần cuối lúc)
- File name (Tên tệp)
- No data (Không có dữ liệu)
- File {1} (Tệp {1})

## `:file.config/`

- The file {1} has the following deprecations: (Tệp {1} có các mục đã lỗi thời sau:)
- The file {1} has the following errors: (Tệp {1} có các lỗi sau:)
- Make sure the config is wrapped in {}. (Đảm bảo cấu hình được bao trong {}.)
- The file {1} is not valid. (Tệp {1} không hợp lệ.)
- Make sure your config is wrapped in {}. Also make sure that the characters '( { [' have their corresponding closing character ') } ]'. (Đảm bảo cấu hình của bạn được bao trong {}. Cũng đảm bảo các ký tự '( { [' có ký tự đóng tương ứng ') } ]'.)
- Failed to read file {1} (Đọc tệp {1} thất bại)

## `:flashcard/`

- Add new query (Thêm truy vấn mới)
- All cards (Tất cả thẻ)
- Select cards (Chọn thẻ)
- Shortcut: {1} (Phím tắt: {1})

## `:flashcard.empty/`

- You can add "{1}" to any block to turn it into a card or trigger "/cloze" to add some clozes. (Bạn có thể thêm "{1}" vào bất kỳ khối nào để biến nó thành thẻ ghi nhớ hoặc dùng "/cloze" để thêm cloze.)
- Time to create a card! (Đến lúc tạo thẻ ghi nhớ rồi!)

## `:flashcard.rating/`

- Again (Lặp lại)
- We got the answer wrong. Automatically means that we have forgotten the card. This is a lapse in memory. (Bạn đã trả lời sai. Tự động có nghĩa là bạn đã quên thẻ. Đây là một lỗ hổng trí nhớ.)
- Easy (Dễ)
- The answer was correct and we were confident and quick in our recall without mental effort. (Bạn trả lời đúng và tự tin, nhanh chóng nhớ ra mà không cần nỗ lực trí óc.)
- Good (Tốt)
- The answer was correct but we took some mental effort to recall it. (Bạn trả lời đúng nhưng cần một chút nỗ lực để nhớ ra.)
- Hard (Khó)
- The answer was correct but we were not confident about it or took too long to recall. (Bạn trả lời đúng nhưng không tự tin hoặc mất quá nhiều thời gian để nhớ.)

## `:flashcard.review/`

- Congrats, you've reviewed all the cards for this query, see you next time! 💯 (Chúc mừng, bạn đã ôn tập xong tất cả thẻ cho truy vấn này, hẹn gặp lại lần sau! 💯)
- Hide answers (Ẩn đáp án)
- Show answers (Hiển thị đáp án)
- Show clozes (Hiển thị cloze)

## `:format/`

- Bold (In đậm)
- Code (Mã)
- Highlight (Tô sáng)
- Italics (In nghiêng)
- Strikethrough (Gạch ngang)

## `:graph/`

- All graphs (Tất cả đồ thị)
- The graph '{1}' already exists. Please try again with another name. (Đồ thị '{1}' đã tồn tại. Vui lòng thử lại với tên khác.)
- as PNG (dưới dạng PNG)
- Built-in pages (Trang tích hợp sẵn)
- Charge Range (Phạm vi lực đẩy)
- Charge Strength (Cường độ lực đẩy)
- Click to search (Nhấn để tìm kiếm)
- Create db graph (Tạo đồ thị db)
- Create graph failed. (Tạo đồ thị thất bại.)
- Create a new graph (Tạo đồ thị mới)
- Created before (Tạo trước)
- Creating graph (Đang tạo đồ thị)
- Delete local graph (Xoá đồ thị cục bộ)
- Are you sure you want to permanently delete the graph "{1}" from Logseq? (Bạn có chắc chắn muốn xoá vĩnh viễn đồ thị "{1}" khỏi Logseq?)
- Delete from server (Xoá khỏi máy chủ)
- Are you sure you want to permanently delete the graph "{1}" from our server? (Bạn có chắc chắn muốn xoá vĩnh viễn đồ thị "{1}" khỏi máy chủ?)
- ⚠️ Notice that we can't recover this graph after being deleted. Make sure you have backups before deleting it. (⚠️ Lưu ý rằng chúng tôi không thể khôi phục đồ thị này sau khi xoá. Đảm bảo bạn đã sao lưu trước khi xoá.)
- Git diff (Git diff)
- downloading (đang tải xuống)
- Encrypt graph data (Mã hoá dữ liệu đồ thị)
- Excluded pages (Trang bị loại trừ)
- Forces (Lực)
- Graph gc successfully! (Dọn dẹp đồ thị thành công!)
- Last opened at: {1} (Lần mở cuối: {1})
- Leave this graph (Rời khỏi đồ thị này)
- Are you sure you want to leave this graph? (Bạn có chắc muốn rời khỏi đồ thị này không?)
- Failed to leave graph. (Không thể rời khỏi đồ thị.)
- Left graph. (Đã rời đồ thị.)
- Link Distance (Khoảng cách liên kết)
- Local graphs: (Đồ thị cục bộ:)
- N hops from selected nodes (N bước từ các nút đã chọn)
- your graph name (tên đồ thị của bạn)
- Nodes (Nút)
- Orphan pages (Trang mồ côi)
- Pause simulation (Tạm dừng mô phỏng)
- preparing (đang chuẩn bị)
- Refresh remote graphs (Làm mới đồ thị từ xa)
- Remote graphs: (Đồ thị từ xa:)
- Removed graph "{1}" (Đã xoá đồ thị "{1}")
- Removed graph "{1}". Redirecting to graph "{2}" (Đã xoá đồ thị "{1}". Đang chuyển hướng đến đồ thị "{2}")
- This graph has been removed from Logseq Sync. (Đồ thị này đã bị xoá khỏi Logseq Sync.)
- Reset Graph (Đặt lại đồ thị)
- Reset Forces (Đặt lại các lực)
- Search (Tìm kiếm)
- Shared by others: (Được chia sẻ bởi người khác:)
- Graph updated! Switching to graph ... (Đồ thị đã cập nhật! Đang chuyển sang đồ thị...)
- Use Logseq Sync (Beta testing) (Dùng Logseq Sync (Bản beta))
- Use Logseq Sync? (Dùng Logseq Sync?)

## `:graph.diagnostics/`

- Failed to compute graph checksum diagnostics. (Không thể tính chẩn đoán checksum của đồ thị.)
- Checksum recomputed. Recomputed: {1}, local: {2}, remote: {3}. Downloaded {4}.edn with {5} blocks and checksum attributes {6}. (Đã tính lại checksum. Tính lại: {1}, cục bộ: {2}, từ xa: {3}. Đã tải xuống {4}.edn với {5} khối và thuộc tính checksum {6}.)
- Unable to compute checksum diagnostics for the current graph. (Không thể tính chẩn đoán checksum cho đồ thị hiện tại.)
- Failed to export client ops SQLite. (Không thể xuất client ops SQLite.)
- Client ops SQLite export failed: invalid payload type {1}. (Xuất client ops SQLite thất bại: kiểu payload không hợp lệ {1}.)
- Client ops SQLite exported: {1}.sqlite (Đã xuất client ops SQLite: {1}.sqlite)
- No graph found. (Không tìm thấy đồ thị.)

## `:graph.page/`

- Show Journals (Hiển thị nhật ký)
- Page graph (Đồ thị trang)

## `:graph.switch/`

- Yes, add another graph (Có, thêm đồ thị khác)
- No matched graphs. Do you want to add another one? (Không có đồ thị khớp. Bạn có muốn thêm đồ thị khác không?)
- Switch to: (Chuyển sang:)
- Select a graph (Chọn một đồ thị)

## `:graph.validation/`

- is no longer supported. (không còn được hỗ trợ.)
- is not used in DB graphs as hiding a property is done in its configuration. (không được dùng trong đồ thị DB vì việc ẩn thuộc tính được thực hiện trong cấu hình của nó.)
- is replaced by #Template and the `Apply template to tags` property. (được thay thế bằng #Template và thuộc tính `Apply template to tags`.)
- is no longer supported. Please use '/' and report bugs on it. (không còn được hỗ trợ. Vui lòng dùng '/' và báo cáo lỗi.)
- is not stored in config for DB graphs. (không được lưu trong cấu hình cho đồ thị DB.)
- is not used in DB graphs as it is always enabled. (không được dùng trong đồ thị DB vì luôn được bật.)
- is not used in DB graphs as there is only markdown mode. (không được dùng trong đồ thị DB vì chỉ có chế độ markdown.)
- is not used in DB graphs as all properties have pages. (không được dùng trong đồ thị DB vì tất cả thuộc tính đều có trang.)
- is not used in DB graphs. (không được dùng trong đồ thị DB.)
- Validation detected {1} invalid block(s). These blocks may be buggy. Attempting to fix invalid blocks. Run validation again to see if they were fixed. (Kiểm tra phát hiện {1} khối không hợp lệ. Các khối này có thể bị lỗi. Đang thử sửa các khối không hợp lệ. Chạy kiểm tra lại để xem có sửa được không.)
- Graph name can't contain following reserved characters: (Tên đồ thị không thể chứa các ký tự dành riêng sau:)
- asterisk (dấu sao)
- backslash (dấu gạch chéo ngược)
- colon (dấu hai chấm)
- double quote (dấu nháy kép)
- forward slash (dấu gạch chéo)
- greater than (dấu lớn hơn)
- hash (dấu thăng)
- less than (dấu nhỏ hơn)
- vertical bar or pipe (dấu gạch đứng)
- plus (dấu cộng)
- question mark (dấu hỏi)
- Your graph is valid! {1} (Đồ thị của bạn hợp lệ! {1})

## `:header/`

- Go back (Quay lại)
- Go forward (Đi tới)
- Highlight recent blocks (Tô sáng các khối gần đây)
- Highlight recent blocks: {1} days ago (Tô sáng các khối gần đây: {1} ngày trước)
- More (Thêm)
- Quit highlight recent blocks (Tắt tô sáng khối gần đây)
- Toggle left sidebar (Bật/tắt thanh bên trái)

## `:help/`

- About Logseq (Về Logseq)
- About (Về)
- Ask the community (Hỏi cộng đồng)
- Awesome Logseq (Awesome Logseq)
- Block reference (Tham chiếu khối)
- Logseq blog (Blog Logseq)
- Bug report (Báo cáo lỗi)
- Changelog (Nhật ký thay đổi)
- Community (Cộng đồng)
- Block context menu (Menu ngữ cảnh khối)
- Right-click bullet (Nhấn chuột phải vào dấu đầu dòng)
- Development (Phát triển)
- Documentation (Tài liệu)
- Feature request (Yêu cầu tính năng)
- Forum community (Diễn đàn cộng đồng)
- Handbook (Sổ tay)
- Learn more (Tìm hiểu thêm)
- Markdown syntax (Cú pháp Markdown)
- Open link in sidebar (Mở liên kết trong thanh bên)
- Shift-click reference (Shift-click vào tham chiếu)
- Privacy policy (Chính sách bảo mật)
- Page reference autocomplete (Tự động hoàn thành tham chiếu trang)
- Release notes (Ghi chú phát hành)
- Roadmap (Lộ trình)
- Search pages/blocks/commands (Tìm trang/khối/lệnh)
- Slash autocomplete (Tự động hoàn thành dấu sổ chéo)
- Getting started (Bắt đầu)
- Submit feedback (Gửi phản hồi)
- Support forum (Diễn đàn hỗ trợ)
- Terms (Điều khoản)
- Terms (Điều khoản)
- Usage (Sử dụng)

## `:help.handbook/`

- Chat on Discord (Trò chuyện trên Discord)
- Ask quick questions, meet fellow users, and learn new workflows. (Đặt câu hỏi nhanh, gặp gỡ người dùng khác và học các quy trình làm việc mới.)
- Copy topic link (Sao chép liên kết chủ đề)
- Current chapter (Chương hiện tại)
- Help categories (Danh mục trợ giúp)
- Home (Trang chủ)
- Handbook link copied! (Đã sao chép liên kết sổ tay!)
- monthly posts (bài đăng hàng tháng)
- Next chapter (Chương tiếp theo)
- Popular topics (Chủ đề phổ biến)
- Prev chapter (Chương trước)
- Resources from {1} (Tài nguyên từ {1})
- Search (Tìm kiếm)
- {1} shortcuts ({1} phím tắt)
- Sync (Đồng bộ)
- Help (Trợ giúp)
- Topics (Chủ đề)
- users online (người dùng đang trực tuyến)
- Visit the forum (Thăm diễn đàn)
- Give feedback, request features, and have in-depth conversations. (Gửi phản hồi, yêu cầu tính năng và trò chuyện chuyên sâu.)
- Writing mode (preview in time) (Chế độ viết (xem trước theo thời gian))

## `:help.shortcuts/`

- View shortcuts and tips (Xem phím tắt và mẹo)
- Keyboard shortcuts (Phím tắt bàn phím)
- Shortcut (Phím tắt)
- Keyboard shortcuts (Phím tắt bàn phím)
- Triggers (Kích hoạt)

## `:icon/`

- Emojis ({1}) (Biểu tượng cảm xúc ({1}))
- Icons ({1}) (Biểu tượng ({1}))
- Matched ({1}) (Khớp ({1}))
- Search all (Tìm tất cả)
- Search emojis (Tìm biểu tượng cảm xúc)
- Search icons (Tìm biểu tượng)
- All (Tất cả)
- Emojis (Biểu tượng cảm xúc)
- Icons (Biểu tượng)

## `:import/`

- Import all tags (Nhập tất cả thẻ)
- Skipped copying asset {1} because it exceeds the 100 MB limit. (Đã bỏ qua sao chép tệp đính kèm {1} vì vượt quá giới hạn 100 MB.)
- Imported {1} of {2} assets. See console for details. (Đã nhập {1} trong {2} tệp đính kèm. Xem chi tiết ở console.)
- Imported {1} assets. (Đã nhập {1} tệp đính kèm.)
- Skipped {1} assets. See console for details. (Đã bỏ qua {1} tệp đính kèm. Xem chi tiết ở console.)
- Block icons can't be imported. Manually import it at the block: {1} (Không thể nhập biểu tượng khối. Hãy nhập thủ công tại khối: {1})
- Can't import block into a non-block entity. Please import block elsewhere. (Không thể nhập khối vào thực thể không phải khối. Vui lòng nhập khối ở nơi khác.)
- Import a DB graph's EDN export into a new DB graph (Nhập tệp xuất EDN của đồ thị DB vào một đồ thị DB mới)
- EDN to DB graph (EDN sang đồ thị DB)
- Import debug transit file into a new DB graph (Nhập tệp transit gỡ lỗi vào một đồ thị DB mới)
- Debug Transit (Debug Transit)
- Default behavior for DB graphs (Hành vi mặc định cho đồ thị DB)
- Empty graph name. (Tên đồ thị trống.)
- Extract inline code snippets as child blocks (Trích xuất các đoạn mã nội tuyến thành khối con)
- Import finished! (Nhập hoàn tất!)
- Import a file-based Logseq graph folder into a new DB graph (Nhập thư mục đồ thị Logseq dạng tệp vào một đồ thị DB mới)
- File to DB graph (Tệp sang đồ thị DB)
- Please specify another name as another graph with this name already exists! (Vui lòng chọn tên khác vì đã có đồ thị khác trùng tên!)
- Graph name (Tên đồ thị)
- To fix a property type, change the property value to the correct type and reimport the graph (Để sửa kiểu thuộc tính, hãy đổi giá trị thuộc tính sang kiểu đúng và nhập lại đồ thị)
- Import detected {1} invalid block(s). These blocks may be buggy when you interact with them. See the javascript console for more. (Nhập phát hiện {1} khối không hợp lệ. Các khối này có thể bị lỗi khi tương tác. Xem chi tiết ở console javascript.)
- The given EDN file is not valid EDN. Please fix and try again. (Tệp EDN đã cho không hợp lệ. Vui lòng sửa và thử lại.)
- Importing (Đang nhập)
- Import failed as the file 'logseq/config.edn' was not found for a Logseq graph. (Nhập thất bại vì không tìm thấy tệp 'logseq/config.edn' của đồ thị Logseq.)
- New graph name (Tên đồ thị mới)
- Import existing notes (Nhập ghi chú có sẵn)
- Imported {1} org file(s) as markdown. Support for org files will be added later. (Đã nhập {1} tệp org dưới dạng markdown. Hỗ trợ tệp org sẽ được thêm sau.)
- Page icons can't be imported. Go to the page {1} to manually import it. (Không thể nhập biểu tượng trang. Hãy vào trang {1} để nhập thủ công.)
- Properties are case insensitive and separated by commas (Thuộc tính không phân biệt chữ hoa/thường và phân tách bằng dấu phẩy)
- e.g. type (ví dụ: type)
- Property should be imported manually (Thuộc tính nên được nhập thủ công)
- e.g. parent (ví dụ: parent)
- Property value has type {1} instead of type {2} (Giá trị thuộc tính có kiểu {1} thay vì kiểu {2})
- Import tag parents from property values (Nhập thẻ cha từ giá trị thuộc tính)
- Import additional tags from property values (Nhập thẻ bổ sung từ giá trị thuộc tính)
- Remove inline tags (Xoá các thẻ nội tuyến)
- Please choose an EDN or a JSON file. (Vui lòng chọn tệp EDN hoặc JSON.)
- Import specific tags (Nhập các thẻ cụ thể)
- Import a zip containing db.sqlite and an assets folder (Nhập zip chứa db.sqlite và thư mục tệp đính kèm)
- SQLite + assets (.zip) (SQLite + tệp đính kèm (.zip))
- The submitted EDN data is invalid! Please fix and try again. (Dữ liệu EDN đã gửi không hợp lệ! Vui lòng sửa và thử lại.)
- Import successful! (Nhập thành công!)
- tag 1, tag 2 (thẻ 1, thẻ 2)
- Tags are case insensitive (Thẻ không phân biệt chữ hoa/thường)
- Import (Nhập)
- Unexpected error: {1} (Lỗi không mong muốn: {1})
- Zip import failed: {1} (Nhập zip thất bại: {1})
- Zip missing db.sqlite. Please check the archive structure. (Zip thiếu db.sqlite. Vui lòng kiểm tra cấu trúc tệp lưu trữ.)

## `:journal/`

- Blocks added to today! (Đã thêm khối vào hôm nay!)
- This page can't be changed to a journal page. (Trang này không thể chuyển sang trang nhật ký.)
- Failed to parse date to journal name. (Không thể phân tích ngày sang tên nhật ký.)

## `:journal.default-query/`

- DOING (ĐANG LÀM)
- TODO (CẦN LÀM)

## `:keymap/`

- All (Tất cả)
- Already bound to this action (Đã được gán cho hành động này)
- Also used for {1} (Cũng được dùng cho {1})
- Also used for {1} in {2} (Cũng được dùng cho {1} trong {2})
- Clear (Xoá)
- Clear keystroke filter (Xoá bộ lọc phím)
- Clear search (Xoá tìm kiếm)
- Custom (Tuỳ chỉnh)
- Deactivates (Vô hiệu hoá)
- Deactivates {1} chord shortcuts (Vô hiệu hoá {1} phím tắt tổ hợp)
- Disabled (Đã tắt)
- Esc is reserved (Esc được dành riêng)
- Cancel (Huỷ)
- Close (Đóng)
- Reassign (Gán lại)
- Remove (Xoá)
- Keystroke filter (Bộ lọc phím)
- No matching shortcuts (Không có phím tắt khớp)
- Press a shortcut (Nhấn một phím tắt)
- Press keys to filter (Nhấn phím để lọc)
- Reassign (Gán lại)
- Remove from the other action and assign here (Xoá khỏi hành động khác và gán vào đây)
- Reassigned from {1} (Gán lại từ {1})
- Refresh all (Làm mới tất cả)
- Remove binding (Xoá gán phím)
- Remove filter (Xoá bộ lọc)
- Reset (Đặt lại)
- Reset to default (Đặt lại về mặc định)
- Search by keys (Tìm kiếm theo phím)
- Search shortcuts... (Tìm phím tắt...)
- Shortcut added (Đã thêm phím tắt)
- Shortcut removed (Đã xoá phím tắt)
- Toggle categories pane (Bật/tắt khung danh mục)
- Undo (Hoàn tác)
- Unset (Bỏ gán)
- Used by {1} (Được dùng bởi {1})

## `:library/`

- Add existing pages to Library (Thêm các trang có sẵn vào Thư viện)
- Add pages (Thêm trang)
- Library (Thư viện)

## `:mobile/`

- Camera access is denied. Enable it in Settings > Logseq. (Quyền truy cập camera bị từ chối. Hãy bật trong Cài đặt > Logseq.)
- Microphone access is denied. Enable it in Settings > Logseq. (Quyền truy cập micro bị từ chối. Hãy bật trong Cài đặt > Logseq.)

## `:mobile.header/`

- Actions (Hành động)
- Create new graph (Tạo đồ thị mới)
- Are you sure to delete this block? (Bạn có chắc muốn xoá khối này không?)
- Are you sure to delete this page? (Bạn có chắc muốn xoá trang này không?)

## `:mobile.intent/`

- File Options (Tuỳ chọn tệp)
- Open file with your favorite app (Mở tệp với ứng dụng yêu thích)
- Select an option to perform (Chọn một tuỳ chọn để thực hiện)
- Share (Chia sẻ)

## `:mobile.log/`

- All (Tất cả)
- Errors only (Chỉ lỗi)
- Full log (Nhật ký đầy đủ)
- New record first (Bản ghi mới trước)
- Old record first (Bản ghi cũ trước)
- UI logs (Nhật ký UI)
- worker logs (Nhật ký worker)

## `:mobile.settings/`

- Account (Tài khoản)
- Check log (Kiểm tra nhật ký)
- Discord community (Cộng đồng Discord)
- Forum (Diễn đàn)
- GitHub (GitHub)
- Report bug (Báo cáo lỗi)
- Revision (Phiên bản)
- Theme (Giao diện)
- Version (Phiên bản)

## `:mobile.share/`

- Failed to import the shared file. Please try again. (Không thể nhập tệp đã chia sẻ. Vui lòng thử lại.)
- Failed to import the shared media. Please try again. (Không thể nhập media đã chia sẻ. Vui lòng thử lại.)
- Parsing current shared content is not supported. Please report the following codes on {1}. We will look into it soon. (Không hỗ trợ phân tích nội dung chia sẻ hiện tại. Vui lòng báo cáo các mã sau trên {1}. Chúng tôi sẽ xem xét sớm.)
- Import {1} file is not supported. You can report it on {2}. We will look into it soon. (Nhập tệp {1} không được hỗ trợ. Bạn có thể báo cáo trên {2}. Chúng tôi sẽ xem xét sớm.)

## `:mobile.tab/`

- Capture (Chụp)
- Go To (Đi tới)
- Graphs (Đồ thị)

## `:mobile.toolbar/`

- Audio (Âm thanh)
- Copy ref (Sao chép tham chiếu)
- Copy url (Sao chép URL)
- Hide (Ẩn)
- Indent (Tăng thụt lề)
- Outdent (Giảm thụt lề)
- Photo (Ảnh)
- Redo (Làm lại)
- Reference (Tham chiếu)
- Slash (Sổ chéo)
- Tag (Thẻ)
- Todo (Cần làm)
- Undo (Hoàn tác)
- Unselect (Bỏ chọn)

## `:nav/`

- All files (Tất cả tệp)
- All journals (Tất cả nhật ký)
- Appearance (Giao diện)
- Assets (Tệp đính kèm)
- Cannot go to an internal page. (Không thể đi đến trang nội bộ.)
- Flashcards (Thẻ ghi nhớ)
- Graph (Đồ thị)
- Graph view (Chế độ đồ thị)
- Help (Trợ giúp)
- Home (Trang chủ)
- Invalid jump (Bước nhảy không hợp lệ)
- Journals (Nhật ký)
- Plugins (Plugin)
- Search (Tìm kiếm)
- Settings (Cài đặt)
- Skip to main content (Đến nội dung chính)
- Tasks (Tác vụ)
- Themes (Giao diện)

## `:nav.all-pages/`

- Pages (Trang)
- Pages (Trang)

## `:node/`

- Built-in nodes can't be deleted. (Không thể xoá các nút tích hợp sẵn.)
- New node (Nút mới)

## `:notification/`

- Clear all (Xoá tất cả)
- Copied! (Đã sao chép!)

## `:onboarding.import/`

- If they are in an EDN or Markdown format Logseq can work with them. (Nếu chúng ở định dạng EDN hoặc Markdown, Logseq có thể làm việc được.)
- Import a SQLite DB Export of your Logseq graph into a new DB graph (Nhập tệp xuất SQLite DB của đồ thị Logseq vào một đồ thị DB mới)
- Do you already have notes that you want to import? (Bạn đã có ghi chú muốn nhập chưa?)

## `:onboarding.import-option/`

- You can also do this later in the app. (Bạn cũng có thể làm việc này sau trong ứng dụng.)
- Import existing notes (Nhập ghi chú có sẵn)

## `:onboarding.setup/`

- First you need to choose a folder where Logseq will store your thoughts, ideas, notes. (Trước hết, bạn cần chọn một thư mục nơi Logseq sẽ lưu suy nghĩ, ý tưởng, ghi chú của bạn.)

## `:page/`

- Add to Favorites (Thêm vào Yêu thích)
- Added tag "{1}" to "{2}" (Đã thêm thẻ "{1}" vào "{2}")
- Backlinks (Backlink)
- Clear (Xoá)
- This does delete minor preferences like dark/light theme preference. (Việc này xoá các tuỳ chọn nhỏ như giao diện sáng/tối.)
- Cleared! (Đã xoá!)
- Contents (Mục lục)
- Convert to Tag (Chuyển thành thẻ)
- Copy page URL (Sao chép URL trang)
- Create a new page (Tạo trang mới)
- Created At (Tạo lúc)
- current graph as SQLite db (đồ thị hiện tại dưới dạng SQLite db)
- Failed default query: (Truy vấn mặc định thất bại:)
- Delete page (Xoá trang)
- Go back home (Quay về trang chủ)
- Hide properties (Ẩn thuộc tính)
- local storage (lưu trữ cục bộ)
- Logseq is having a problem. To try to get it back to a working state, please try the following safe steps in order: (Logseq đang gặp sự cố. Để đưa nó về trạng thái hoạt động, vui lòng thử các bước an toàn theo thứ tự sau:)
- Make it private (Đặt riêng tư)
- Make it public for publishing (Đặt công khai để xuất bản)
- Node has been moved to Recycle (Đã chuyển nút vào Thùng rác)
- Page name (Tên trang)
- No page found to copy (Không tìm thấy trang để sao chép)
- Page not found (Không tìm thấy trang)
- Oops! The page you're looking for doesn't exist. (Rất tiếc! Trang bạn đang tìm không tồn tại.)
- Page Not Found (Không tìm thấy trang)
- No page found (Không tìm thấy trang)
- You can also go to {{All graphs}} to switch to another graph. (Bạn cũng có thể vào {{All graphs}} để chuyển sang đồ thị khác.)
- If these troubleshooting steps have not solved your problem, please {{open an issue}}. (Nếu các bước khắc phục không giải quyết được vấn đề, vui lòng {{open an issue}}.)
- Open properties (Mở thuộc tính)
- Rebuild (Xây dựng lại)
- Relaunch (Khởi động lại)
- Quit and reopen the app. (Thoát và mở lại ứng dụng.)
- Scheduled and Deadline (Đã lên lịch và hạn chót)
- search index (chỉ mục tìm kiếm)
- You can send it to help@logseq.com for debugging. (Bạn có thể gửi đến help@logseq.com để gỡ lỗi.)
- Something went wrong (Đã có lỗi xảy ra)
- Step {1} (Bước {1})
- the app (ứng dụng)
- Try (Thử)
- Unfavorite page (Bỏ yêu thích trang)
- Unknown page (Trang không xác định)
- Updated At (Cập nhật lúc)

## `:page.convert/`

- Can't convert this block to page since its parent is not a page. (Không thể chuyển khối này thành trang vì khối cha không phải trang.)
- Page "{1}" cannot be converted to a block. (Trang "{1}" không thể chuyển thành khối.)
- Page "{1}" cannot be converted to a block because it has page children. (Trang "{1}" không thể chuyển thành khối vì có các trang con.)
- Page "{1}" cannot be converted to a block, please move it to another page first. (Trang "{1}" không thể chuyển thành khối, vui lòng di chuyển sang trang khác trước.)
- Convert "{1}" to tag (Chuyển "{1}" thành thẻ)
- Built-in pages can't be used as tags (Trang tích hợp sẵn không thể dùng làm thẻ)
- A tag with the name "{1}" already exists. (Đã có thẻ tên "{1}".)
- Namespaced pages can't be tags (Trang có namespace không thể là thẻ)
- Can't convert property value to page. (Không thể chuyển giá trị thuộc tính thành trang.)
- Convert Tag to Page (Chuyển thẻ thành trang)
- Built-in tags can't be converted to pages (Thẻ tích hợp sẵn không thể chuyển thành trang)
- Converting a tag to page also removes its tag properties and its tag from all nodes tagged with it. Are you ok with that? (Việc chuyển thẻ thành trang cũng xoá các thuộc tính thẻ và xoá thẻ khỏi tất cả các nút đã gắn thẻ. Bạn có đồng ý không?)
- A page with the name "{1}" already exists. (Đã có trang tên "{1}".)
- This tag cannot be converted because it has tag children. All tag children must be removed or converted before converting this tag. (Thẻ này không thể chuyển vì có các thẻ con. Tất cả thẻ con phải được xoá hoặc chuyển trước khi chuyển thẻ này.)

## `:page.delete/`

- Are you sure you want to delete these pages? Properties and tags will be permanently deleted and pages will be moved to Recycle. (Bạn có chắc muốn xoá các trang này? Thuộc tính và thẻ sẽ bị xoá vĩnh viễn và các trang sẽ được chuyển vào Thùng rác.)
- Are you sure you want to delete this page? (Bạn có chắc muốn xoá trang này không?)
- Are you sure you want to permanently delete this page? (Bạn có chắc muốn xoá vĩnh viễn trang này không?)
- Page "{1}" was deleted successfully! (Đã xoá trang "{1}" thành công!)
- Total: {1} (Tổng: {1})
- These pages had their content deleted but were unable to be deleted: {1}. See javascript console for more details. (Các trang này đã bị xoá nội dung nhưng không thể xoá hoàn toàn: {1}. Xem chi tiết ở console javascript.)

## `:page.validation/`

- New page can't set built-in tags: {1} (Trang mới không thể đặt các thẻ tích hợp sẵn: {1})
- Another page named "{1}" already exists for tags: {2}. (Đã có trang tên "{1}" cho các thẻ: {2}.)
- Page name can't be blank. (Tên trang không thể để trống.)
- Page name can't include "#". (Tên trang không thể chứa "#".)
- Page name can't include "/". (Tên trang không thể chứa "/".)
- Cannot create this page unless all parents are pages. (Không thể tạo trang này trừ khi tất cả trang cha đều là trang.)

## `:pdf/`

- Annotations page (Trang chú thích)
- Area highlight ({1}) (Tô sáng vùng ({1}))
- Auto fit (Tự động vừa khít)
- Auto open context menu for selections (Tự động mở menu ngữ cảnh khi chọn)
- Copy ref (Sao chép tham chiếu)
- Copy text (Sao chép văn bản)
- Error: {1}
Is this .pdf file corrupted?
Please confirm with external pdf viewer. (Lỗi: {1}
Tệp .pdf này có bị hỏng không?
Vui lòng kiểm tra với trình xem PDF khác.)
- Document metadata (Metadata tài liệu)
- Enter to search (Nhấn Enter để tìm kiếm)
- {1} of {2} matches ("{3}") ({1} trong {2} kết quả khớp ("{3}"))
- Error: {1}
{2}
Please confirm with pdf file resource. (Lỗi: {1}
{2}
Vui lòng kiểm tra với tài nguyên tệp PDF.)
- Highlight mode (Chế độ tô sáng)
- Highlights (Tô sáng)
- Colored label for highlight block (Nhãn màu cho khối tô sáng)
- Linked references (Tham chiếu liên kết)
- Error: failed to load the highlights file: "{1}". 
{2} (Lỗi: không thể tải tệp tô sáng: "{1}". 
{2})
- Error: {1}
 Is this the correct path? (Lỗi: {1}
 Đường dẫn này có đúng không?)
- More settings (Cài đặt thêm)
- No outlines (Không có dàn ý)
- Not found. (Không tìm thấy.)
- Open in app window (Mở trong cửa sổ ứng dụng)
- Open in external window (Mở trong cửa sổ bên ngoài)
- Outline (Dàn ý)
- Page {1} (Trang {1})
- This document is password protected. Please enter a password: (Tài liệu này được bảo vệ bằng mật khẩu. Vui lòng nhập mật khẩu:)
- Password required (Yêu cầu mật khẩu)
- Search (Tìm kiếm)
- search (tìm kiếm)
- Dashed style for area highlight (Kiểu nét đứt cho tô sáng vùng)
- viewer pdf (trình xem PDF)
- Zoom in (Phóng to)
- Zoom out (Thu nhỏ)

## `:plugin/`

- All (Tất cả)
- Auto check for updates (Tự động kiểm tra cập nhật)
- Auto check for updates: {1}! (Tự động kiểm tra cập nhật: {1}!)
- Check all updates (Kiểm tra tất cả cập nhật)
- Check update (Kiểm tra cập nhật)
- Checked (Đã kiểm tra)
- Checking for plugin updates ... (Đang kiểm tra cập nhật plugin...)
- ✨ Write and submit new plugin (✨ Viết và gửi plugin mới)
- Found the custom.js file, is it allowed to execute? (If you don't understand the content of this file, it is recommended not to allow execution, which has certain security risks.) (Đã tìm thấy tệp custom.js, có cho phép thực thi không? (Nếu bạn không hiểu nội dung tệp này, khuyến nghị không cho phép thực thi vì có rủi ro bảo mật.))
- Newly added (Mới thêm)
- Are you sure you want to uninstall the plugin [{1}]? (Bạn có chắc muốn gỡ cài plugin [{1}]?)
- The plugin {1} is disabled. (Plugin {1} đã bị tắt.)
- Disable now (Tắt ngay)
- Disabled (Đã tắt)
- Does not support DB graphs (Không hỗ trợ đồ thị DB)
- Downloads (Lượt tải)
- Nothing Found. (Không tìm thấy gì.)
- Enabled (Đã bật)
- Existed plugin package ({1}). (Gói plugin đã tồn tại ({1}).)
- Fatal: {1} (Lỗi nghiêm trọng: {1})
- Found {1} updates (Tìm thấy {1} cập nhật)
- New updates (Có cập nhật mới)
- Install (Cài đặt)
- Install failed: {1}
{2} (Cài đặt thất bại: {1}
{2})
- Installed (Đã cài đặt)
- Installed plugin: {1} (Đã cài plugin: {1})
- Installing (Đang cài đặt)
- Invalid GitHub repo url (URL GitHub repo không hợp lệ)
- Illegal Logseq plugin package. (Gói plugin Logseq không hợp lệ.)
- Invalid plugins.edn provided. See javascript console for specific errors (plugins.edn không hợp lệ. Xem console javascript để biết lỗi cụ thể)
- Plugin Updates:  (Cập nhật plugin: )
- Load plugin from web url (Tải plugin từ URL web)
- Load plugin: {1}... (Tải plugin: {1}...)
- Load unpacked plugin (Tải plugin chưa đóng gói)
- LOADING (ĐANG TẢI)
- Clear (Xoá)
- Logs copied to clipboard. (Đã sao chép nhật ký vào clipboard.)
- Copy all (Sao chép tất cả)
- No logs yet. (Chưa có nhật ký nào.)
- Filter logs... (Lọc nhật ký...)
- All levels (Tất cả mức)
- Plugin logs (Nhật ký plugin)
- Malformed plugins.edn provided. Please check the file has correct edn syntax. (plugins.edn bị hỏng. Vui lòng kiểm tra cú pháp edn của tệp.)
- Marketplace (Cửa hàng)
- New plugin registered! (Đã đăng ký plugin mới!)
- No Settings Schema! (Không có Schema cài đặt!)
- Not installed (Chưa cài đặt)
- View logs (Xem nhật ký)
- Open (Mở)
- Open package (Mở gói)
- Open Preferences (Mở Tuỳ chọn)
- Open settings (Mở cài đặt)
- This plugin {1} takes too long to load, affecting the application startup time and potentially causing other plugins to fail to load. (Plugin {1} mất quá nhiều thời gian để tải, ảnh hưởng đến thời gian khởi động ứng dụng và có thể khiến các plugin khác không tải được.)
- Popular (Phổ biến)
- Success! Status {1} in {2}ms. (Thành công! Trạng thái {1} trong {2}ms.)
- No README content. (Không có nội dung README.)
- Refresh lists (Làm mới danh sách)
- Reload (Tải lại)
- Remote error: {1} (Lỗi từ xa: {1})
- If any plugin is unavailable or you think it contains malicious code, please email {1}. Mention the name of the plugin and the URL of its GitHub repository. The Logseq team usually responds within a business day. (Nếu plugin nào không khả dụng hoặc bạn nghĩ nó chứa mã độc hại, hãy gửi email đến {1}. Đề cập tên plugin và URL repo GitHub. Đội Logseq thường phản hồi trong vòng một ngày làm việc.)
- Report plugin (Báo cáo plugin)
- Restart App (Khởi động lại ứng dụng)
- Search plugins (Tìm kiếm plugin)
- Plugins can access your graph and your local files, issue network requests.
They can also cause data corruption or loss. We're working on proper access rules for your graphs.
Meanwhile, make sure you have regular backups of your graphs and only install the plugins when you can read and
understand the source code. (Plugin có thể truy cập đồ thị, tệp cục bộ và tạo yêu cầu mạng.
Chúng cũng có thể gây hỏng hoặc mất dữ liệu. Chúng tôi đang hoàn thiện các quy tắc truy cập đúng cho đồ thị của bạn.
Trong lúc đó, hãy đảm bảo bạn sao lưu đồ thị thường xuyên và chỉ cài plugin khi bạn có thể đọc và hiểu mã nguồn.)
- #Not Handled# {1} (#Chưa xử lý# {1})
- Settings schema Error! (Lỗi Schema cài đặt!)
- Stars (Sao)
- Supports DB graphs (Hỗ trợ đồ thị DB)
- Title ({1}) (Tiêu đề ({1}))
- Uninstall (Gỡ cài đặt)
- Unpacked (Chưa đóng gói)
- Select the plugin directory (Chọn thư mục plugin)
- It's up to date {1} (Đã cập nhật {1})
- Update (Cập nhật)
- Update all of selected (Cập nhật tất cả các plugin đã chọn)
- All updated! (Đã cập nhật tất cả!)
- Update available (Có cập nhật)
- Update plugin: {1} - {2} (Cập nhật plugin: {1} - {2})
- Downloading updates (Đang tải cập nhật)
- Updating (Đang cập nhật)

## `:plugin.install-from-file/`

- Install from plugins.edn (Cài từ plugins.edn)
- The following plugins will replace your plugins: (Các plugin sau sẽ thay thế các plugin của bạn:)
- All plugins installed! (Đã cài tất cả plugin!)
- Install plugins from plugins.edn (Cài plugin từ plugins.edn)

## `:plugin.install-from-web-url/`

- effect (hiệu ứng)
- GitHub repo url (URL GitHub repo)
- URLs support both GitHub repositories and local development servers. (For Examples: {1}, {2}) (URL hỗ trợ cả GitHub repo và máy chủ phát triển cục bộ. (Ví dụ: {1}, {2}))
- theme (giao diện)

## `:plugin.package-config/`

- Removing it only detaches the plugin from Logseq and keeps the source folder untouched. (Việc xoá chỉ tách plugin khỏi Logseq và giữ nguyên thư mục nguồn.)
- Failed to parse the plugin package config. (Không thể phân tích cấu hình gói plugin.)
- Failed to remove the broken plugin. (Không thể xoá plugin bị hỏng.)
- Removed the broken plugin from the plugin list. (Đã xoá plugin bị hỏng khỏi danh sách plugin.)
- Removed broken plugin "{1}". (Đã xoá plugin bị hỏng "{1}".)

## `:plugin.proxy/`

- Direct (Trực tiếp)
- System (Hệ thống)
- Test URL (URL kiểm tra)
- Testing (Đang kiểm tra)

## `:plugin.settings/`

- Edit settings.json (Sửa settings.json)
- Exit code mode (Thoát chế độ mã)
- Plugins (Plugin)

## `:plugin.themes/`

- Logseq default {1} theme. (Giao diện {1} mặc định của Logseq.)
- Default {1} Theme (Giao diện {1} mặc định)
- {1} themes (Giao diện {1})
- light & dark themes (giao diện sáng & tối)

## `:profiler/`

- input fn name here (nhập tên hàm tại đây)
- Unregister (Huỷ đăng ký)

## `:property/`

- Add choice (Thêm lựa chọn)
- Add choices (Thêm các lựa chọn)
- Add description (Thêm mô tả)
- Add property (Thêm thuộc tính)
- Add or change property (Thêm hoặc thay đổi thuộc tính)
- Available choices (Các lựa chọn có sẵn)
- Change {1} (Thay đổi {1})
- Checkbox state mapping (Ánh xạ trạng thái checkbox)
- Children ({1}) (Con ({1}))
- {1} choices ({1} lựa chọn)
- Choose tag (Chọn thẻ)
- Choose tags (Chọn các thẻ)
- Clear (Xoá)
- Configure property (Cấu hình thuộc tính)
- Convert "{1}" to property (Chuyển "{1}" thành thuộc tính)
- Property failed to create. Please try a different property name. (Tạo thuộc tính thất bại. Vui lòng thử tên thuộc tính khác.)
- Default value (Giá trị mặc định)
- Delete property from node (Xoá thuộc tính khỏi nút)
- Are you sure you want to delete the property from this node? (Bạn có chắc muốn xoá thuộc tính khỏi nút này không?)
- Delete property from tag (Xoá thuộc tính khỏi thẻ)
- Are you sure you want to delete the property from this tag? (Bạn có chắc muốn xoá thuộc tính khỏi thẻ này không?)
- description (mô tả)
- Drag && Drop to reorder (Kéo && Thả để sắp xếp lại)
- Existing values: (Các giá trị hiện có:)
- Go to this property (Đến thuộc tính này)
- Hidden properties (Thuộc tính ẩn)
- Hide by default (Ẩn mặc định)
- Hide choice for this tag (Ẩn lựa chọn cho thẻ này)
- Hide empty value (Ẩn giá trị trống)
- Hide for #{1} (Ẩn cho #{1})
- Hide hidden choices (Ẩn các lựa chọn đã ẩn)
- Map checked to (Ánh xạ đã chọn thành)
- Map unchecked to (Ánh xạ chưa chọn thành)
- More settings (Cài đặt thêm)
- Multiple values (Nhiều giá trị)
- This action cannot be undone. Do you want to change this property to have multiple values? (Hành động này không thể hoàn tác. Bạn có muốn đổi thuộc tính này thành nhiều giá trị không?)
- Property name (Tên thuộc tính)
- name (tên)
- Nodes with property (Các nút có thuộc tính)
- Overdue (Quá hạn)
- This is a private built-in property that can't be used. (Đây là thuộc tính tích hợp sẵn riêng tư, không thể sử dụng.)
- Remove scope for #{1} (Xoá phạm vi cho #{1})
- Only for #{1} (Chỉ cho #{1})
- Select a choice (Chọn một lựa chọn)
- Select a property (Chọn một thuộc tính)
- Select a property type (Chọn kiểu thuộc tính)
- Self reference (Tự tham chiếu)
- Set as default choice (Đặt làm lựa chọn mặc định)
- Set default value (Đặt giá trị mặc định)
- Set Icon (Đặt biểu tượng)
- Set {1} (Đặt {1})
- Set property (Đặt thuộc tính)
- Set tags (Đặt thẻ)
- Set value (Đặt giá trị)
- Show as checkbox on node (Hiển thị dưới dạng checkbox trên nút)
- Show as checkbox on tagged nodes (Hiển thị dưới dạng checkbox trên các nút được gắn thẻ)
- Show hidden choices (Hiển thị các lựa chọn ẩn)
- Skip choosing tag (Bỏ qua chọn thẻ)
- Specify node tags (Chỉ định thẻ của nút)
- title (tiêu đề)
- Property type (Kiểu thuộc tính)
- Changing the property type clears some property configurations. (Việc đổi kiểu thuộc tính sẽ xoá một số cấu hình thuộc tính.)
- Checkbox (Checkbox)
- Date (Ngày)
- DateTime (Ngày giờ)
- The type of this property is locked once you start using it. This is to make sure all your existing information stays correct if the property type is changed later. To unlock, all uses of a property must be deleted. (Kiểu thuộc tính bị khoá khi bạn bắt đầu sử dụng. Điều này đảm bảo thông tin hiện có vẫn đúng nếu kiểu thay đổi sau. Để mở khoá, phải xoá tất cả việc dùng thuộc tính.)
- Node (Nút)
- Number (Số)
- Text (Văn bản)
- URL (URL)
- UI position (Vị trí UI)
- Below the block (Dưới khối)
- Beginning of the block (Đầu khối)
- End of the block (Cuối khối)
- Block properties (Thuộc tính khối)
- Unset property (Bỏ đặt thuộc tính)
- Property updated! (Đã cập nhật thuộc tính!)
- Use in #{1} (Dùng trong #{1})

## `:property.built-in/`

- Alias (Bí danh)
- Asset (Tệp đính kèm)
- Asset alignment (Căn chỉnh tệp đính kèm)
- File checksum (Checksum tệp)
- External file name (Tên tệp bên ngoài)
- External URL (URL bên ngoài)
- Image height (Chiều cao hình ảnh)
- Last visit page (Trang ghé thăm gần nhất)
- File remote metadata (Metadata tệp từ xa)
- Asset resize metadata (Metadata thay đổi kích thước tệp)
- File Size (Kích thước tệp)
- File Type (Loại tệp)
- Image width (Chiều rộng hình ảnh)
- Background color (Màu nền)
- Built in? (Tích hợp sẵn?)
- Properties displayed as checkbox (Thuộc tính hiển thị dưới dạng checkbox)
- Choice checkbox state (Trạng thái checkbox của lựa chọn)
- Choice classes (Lớp của lựa chọn)
- Choice exclusions (Loại trừ lựa chọn)
- Bidirectional property title (Tiêu đề thuộc tính hai chiều)
- Enable bidirectional properties (Bật thuộc tính hai chiều)
- Extends (Kế thừa từ)
- Hide from Node (Ẩn khỏi nút)
- Tag Properties (Thuộc tính của thẻ)
- Property classes (Lớp thuộc tính)
- Closed value property (Thuộc tính giá trị đóng)
- Code Mode (Chế độ mã)
- Node collapsed? (Nút đã thu gọn?)
- Node created at (Nút tạo lúc)
- Node created by (Nút được tạo bởi)
- Created from property (Tạo từ thuộc tính)
- Deadline (Hạn chót)
- Default value (Giá trị mặc định)
- Deleted at (Xoá lúc)
- Deleted by (Xoá bởi)
- Description (Mô tả)
- Enable property history (Bật lịch sử thuộc tính)
- Excluded from Graph view? (Loại trừ khỏi chế độ đồ thị?)
- Due (Đến hạn)
- State (Trạng thái)
- Heading (Tiêu đề)
- Hide this property or page (Ẩn thuộc tính hoặc trang này)
- Hide empty value (Ẩn giá trị trống)
- History block (Khối lịch sử)
- History property (Thuộc tính lịch sử)
- History value (Giá trị lịch sử)
- History scalar value (Giá trị vô hướng lịch sử)
- Icon (Biểu tượng)
- Journal date (Ngày nhật ký)
- Title Format (Định dạng tiêu đề)
- Node links to (Nút liên kết đến)
- Excluded references (Tham chiếu loại trừ)
- Included references (Tham chiếu bao gồm)
- LS type (Kiểu LS)
- Node Display Type (Kiểu hiển thị nút)
- Node order (Thứ tự nút)
- List type (Kiểu danh sách)
- Node page (Trang nút)
- Page Tags (Thẻ trang)
- Node parent (Nút cha)
- Priority (Độ ưu tiên)
- Property public? (Thuộc tính công khai?)
- Published URL (URL đã xuất bản)
- Publishing Public? (Xuất bản công khai?)
- Query (Truy vấn)
- Reaction emoji (Biểu tượng phản hồi)
- Reaction target (Đích phản hồi)
- Recycle original order (Thứ tự gốc trong thùng rác)
- Recycle original page (Trang gốc trong thùng rác)
- Recycle original parent (Nút cha gốc trong thùng rác)
- Node references (Tham chiếu của nút)
- Repeating Checked Property (Thuộc tính lặp đã đánh dấu)
- Repeating recur frequency (Tần suất lặp)
- Repeating recur unit (Đơn vị lặp)
- Node Repeats? (Nút lặp lại?)
- Repeating Temporal Property (Thuộc tính thời gian lặp)
- Non ref type default value (Giá trị mặc định kiểu không tham chiếu)
- Scheduled (Đã lên lịch)
- Status (Trạng thái)
- Reference to large block title stored in remote object storage (Tham chiếu đến tiêu đề khối lớn lưu ở object storage từ xa)
- View filters (Bộ lọc view)
- View hidden columns (Cột bị ẩn của view)
- View ordered columns (Cột đã sắp xếp của view)
- Table view pinned columns (Cột ghim của bảng)
- View columns settings (Cài đặt cột của view)
- View sorting (Sắp xếp view)
- Tags (Thẻ)
- Apply template to tags (Áp dụng mẫu cho thẻ)
- Node title (Tiêu đề nút)
- Property type (Kiểu thuộc tính)
- Property position (Vị trí thuộc tính)
- Node updated at (Nút cập nhật lúc)
- Used template (Mẫu đã dùng)
- User Avatar (Ảnh đại diện)
- User Email (Email người dùng)
- User Name (Tên người dùng)
- Property value (Giá trị thuộc tính)
- Property view context (Ngữ cảnh view của thuộc tính)
- View Feature Type (Kiểu tính năng view)
- This view belongs to (View này thuộc về)
- View group by property (View nhóm theo thuộc tính)
- View sort groups by (View sắp xếp nhóm theo)
- View sort groups DESC (View sắp xếp nhóm DESC)
- View Type (Kiểu view)

## `:property.choice/`

- Choice already exists. (Lựa chọn đã tồn tại.)
- The choice can't be deleted because it's built-in. (Không thể xoá lựa chọn vì là tích hợp sẵn.)
- Invalid choice "{1}" for this property: {2} (Lựa chọn không hợp lệ "{1}" cho thuộc tính: {2})

## `:property.color/`

- blue (xanh dương)
- green (xanh lá)
- purple (tím)
- red (đỏ)
- yellow (vàng)

## `:property.priority/`

- High (Cao)
- Low (Thấp)
- Medium (Trung bình)
- Urgent (Khẩn cấp)

## `:property.repeat/`

- Repeat date (Ngày lặp)
- Repeat datetime (Ngày giờ lặp)
- Every (Mỗi)
- is: (là:)
- Repeat task (Tác vụ lặp)
- When (Khi)

## `:property.repeat-recur-unit/`

- Day (Ngày)
- Hour (Giờ)
- Minute (Phút)
- Month (Tháng)
- Week (Tuần)
- Year (Năm)

## `:property.status/`

- Backlog (Chờ)
- Canceled (Đã huỷ)
- Doing (Đang làm)
- Done (Hoàn thành)
- In Review (Đang xem xét)
- Todo (Cần làm)

## `:property.validation/`

- Can't convert "{1}" to a number. (Không thể chuyển "{1}" thành số.)
- Can't remove required property. (Không thể xoá thuộc tính bắt buộc.)
- Can't set this block itself as own property value. (Không thể đặt khối này làm giá trị thuộc tính của chính nó.)
- Another property named "{1}" already exists. (Đã có thuộc tính khác tên "{1}".)
- This is an invalid property name. A property name cannot start with page reference characters '#' or '[['. (Tên thuộc tính không hợp lệ. Tên thuộc tính không thể bắt đầu bằng ký tự tham chiếu trang '#' hoặc '[['.)
- Property "{1}" has invalid value: {2} (Thuộc tính "{1}" có giá trị không hợp lệ: {2})
- This property can't change from multiple values to one value because it has existing data. (Thuộc tính này không thể đổi từ nhiều giá trị thành một giá trị vì đã có dữ liệu.)
- Property is protected and can't be deleted. (Thuộc tính được bảo vệ và không thể xoá.)

## `:property.view-type/`

- Gallery View (Chế độ Thư viện)
- List View (Chế độ Danh sách)
- Table View (Chế độ Bảng)

## `:publish/`

- Publish (Xuất bản)
- Optionally protect this page with a password. Leave empty for public access. (Tuỳ chọn bảo vệ trang này bằng mật khẩu. Để trống để công khai.)
- Publish page (Xuất bản trang)
- Publish failed: invalid page. (Xuất bản thất bại: trang không hợp lệ.)
- Optional password (Mật khẩu (không bắt buộc))
- Publish failed. (Xuất bản thất bại.)
- Published to: {1} (Đã xuất bản đến: {1})
- Publishing... (Đang xuất bản...)
- Unpublish (Huỷ xuất bản)
- Unpublish failed. (Huỷ xuất bản thất bại.)
- Unpublish failed: missing page id. (Huỷ xuất bản thất bại: thiếu page id.)
- Unpublished. (Đã huỷ xuất bản.)

## `:query/`

- Advanced query results (Kết quả truy vấn nâng cao)
- Custom view error: {1} (Lỗi view tuỳ chỉnh: {1})
- Query error: (Lỗi truy vấn:)
- Check more examples at {{Queries documentation}}. (Xem thêm ví dụ tại {{Queries documentation}}.)
- Query examples: (Ví dụ truy vấn:)
- Results for {1} (Kết quả cho {1})

## `:query.builder/`

- Add filter/operator (Thêm bộ lọc/toán tử)
- End date (Ngày kết thúc)
- between: {1} ~ {2} (giữa: {1} ~ {2})
- Start date (Ngày bắt đầu)
- Created (Đã tạo)
- Filter (Bộ lọc)
- Full text search (Tìm kiếm toàn văn)
- Page (Trang)
- Page reference (Tham chiếu trang)
- Sample (Mẫu)
- and (và)
- not (không)
- Replace with: (Thay bằng:)
- Search: {1} (Tìm: {1})
- Show built-in properties (Hiển thị thuộc tính tích hợp sẵn)
- Unwrap (Bỏ bao)
- Updated (Đã cập nhật)
- Wrap this filter with: (Bao bộ lọc bằng:)

## `:reference/`

- Block references (Tham chiếu khối)
- Copy this reference (Sao chép tham chiếu này)
- Delete this reference (Xoá tham chiếu này)
- Page filter (Bộ lọc trang)
- Replace with embed (Thay bằng nhúng)
- Replace with text (Thay bằng văn bản)

## `:reference.filter/`

- Click to include and shift-click to exclude. Click again to remove. (Nhấn để thêm và shift-click để loại trừ. Nhấn lại để xoá.)
- Excludes:  (Loại trừ: )
- Includes:  (Bao gồm: )
- Search in linked pages (Tìm trong các trang đã liên kết)
- Filter (Bộ lọc)

## `:search/`

- Blank input (Đầu vào trống)
- Full text search (Tìm kiếm toàn văn)
- Indexing {1}% (Đang lập chỉ mục {1}%)
- Search indices rebuilt successfully! (Xây dựng lại chỉ mục tìm kiếm thành công!)
- No matched result (Không có kết quả khớp)

## `:search.find-in-page/`

- Find in page (Tìm trong trang)
- Match case (Phân biệt chữ hoa/thường)
- Next result (Kết quả tiếp theo)
- Previous result (Kết quả trước)

## `:select/`

- Select one (Chọn một)
- Select one or multiple (Chọn một hoặc nhiều)
- + New option: {1} (+ Tuỳ chọn mới: {1})

## `:server/`

- [Server] {1} ([Server] {1})
- MCP URL copied to clipboard! (Đã sao chép URL MCP vào clipboard!)
- Start server (Khởi động server)
- Stop server (Dừng server)
- HTTP API server (HTTP API server)

## `:server.config/`

- Auto start server with the app launched (Tự động khởi động server khi mở ứng dụng)
- Port (1 ~ 65535) (Cổng (1 ~ 65535))
- Reset (Đặt lại)
- Save & Apply (Lưu & áp dụng)
- Server configurations (Cấu hình server)

## `:server.status/`

- Closed (Đã đóng)
- Closing (Đang đóng)
- Error (Lỗi)
- Running (Đang chạy)
- Starting (Đang khởi động)
- Stopped (Đã dừng)

## `:server.token/`

- + Add new token (+ Thêm token mới)
- name (tên)
- Regenerate token value (Tạo lại giá trị token)
- Authorization tokens (Token uỷ quyền)
- Update tokens successfully! (Cập nhật token thành công!)
- value (giá trị)

## `:settings/`

- Account (Tài khoản)
- Advanced (Nâng cao)
- AI (AI)
- Collaboration (Cộng tác)
- Editor (Trình soạn thảo)
- Encryption (Mã hoá)
- Features (Tính năng)
- General (Chung)
- Keymap (Phím tắt)
- Plugins (Plugin)

## `:settings-page/`

- Publish Server URL (URL Publish Server)
- Publish server URL cleared. Using official Logseq Publish. (Đã xoá URL publish server. Đang dùng Logseq Publish chính thức.)
- Logseq Publish (Logseq Publish)
- Set a custom HTTPS publish server URL for self-hosted single-page publishing. Your Logseq authentication tokens will be sent to this server, so only use a trusted URL. Leave empty to use the official Logseq Publish service. (Đặt URL HTTPS publish server tuỳ chỉnh để tự lưu trữ xuất bản trang. Token xác thực Logseq sẽ được gửi đến server này, vì vậy chỉ dùng URL đáng tin cậy. Để trống để dùng dịch vụ Logseq Publish chính thức.)
- Reset to default (Đặt lại về mặc định)
- Publish server URL saved. (Đã lưu URL publish server.)

## `:settings.account/`

- {1}GB of {2}GB total storage ({3}) ({1}GB trên tổng {2}GB ({3}))
- {1} of {2} synced graphs ({3}) ({1} trên {2} đồ thị đã đồng bộ ({3}))

## `:settings.advanced/`

- Automatically change file permissions (Tự động thay đổi quyền tệp)
- Disable to allow editing by multiple users with permissions granted by group membership. (Tắt để cho phép nhiều người dùng chỉnh sửa với quyền được cấp qua thành viên nhóm.)
- Auto updater (Tự động cập nhật)
- Developer mode (Chế độ nhà phát triển)
- Developer mode helps contributors and extension developers test their integrations with Logseq more efficiently. (Chế độ nhà phát triển giúp người đóng góp và nhà phát triển extension kiểm thử tích hợp với Logseq hiệu quả hơn.)
- Send usage data and diagnostics to Logseq (Gửi dữ liệu sử dụng và chẩn đoán cho Logseq)
- Logseq will never collect your local graph database or sell your data. (Logseq sẽ không bao giờ thu thập cơ sở dữ liệu đồ thị cục bộ hoặc bán dữ liệu của bạn.)
- Network proxy (Proxy mạng)

## `:settings.ai/`

- MCP Server (MCP Server)
- Enable MCP Server to allow commmunication with local AI applications. MCP Server depends on HTTP API server. (Bật MCP Server để cho phép giao tiếp với các ứng dụng AI cục bộ. MCP Server phụ thuộc vào HTTP API server.)

## `:settings.editor/`

- Expand block references automatically when zoom-in (Tự động mở rộng tham chiếu khối khi phóng to)
- This option controls whether to expand the block references automatically when zoom-in. (Tuỳ chọn này điều khiển việc tự động mở rộng tham chiếu khối khi phóng to.)
- Preferred date format (Định dạng ngày ưa thích)
- All pages public when publishing (Tất cả trang công khai khi xuất bản)
- Enable shortcut tooltip (Bật tooltip phím tắt)
- Tooltips (Tooltip)
- Logical outdenting (Giảm thụt lề logic)
- The left side shows outdenting with the default setting, and the right shows outdenting with logical outdenting enabled  (Bên trái hiển thị giảm thụt lề mặc định, bên phải hiển thị khi bật giảm thụt lề logic)
- → Learn more (→ Tìm hiểu thêm)
- Prefer pasting file (Ưu tiên dán tệp)
- When enabled, pasting an image from the internet will download and insert the image. When disabled, it will paste the link to the image. (Khi bật, dán hình ảnh từ Internet sẽ tải xuống và chèn hình ảnh. Khi tắt, sẽ dán liên kết đến hình ảnh.)
- Show brackets (Hiển thị ngoặc vuông)
- Show all lines of a block reference (Hiển thị tất cả các dòng của tham chiếu khối)
- Spell checker (Kiểm tra chính tả)
- Wide mode (Chế độ rộng)

## `:settings.features/`

- Flashcards (Thẻ ghi nhớ)
- Journals (Nhật ký)
- Set the default home page (Đặt trang chủ mặc định)
- Home default page updated successfully! (Đã cập nhật trang chủ mặc định!)
- Journals enabled (Đã bật nhật ký)
- To access new features before anyone else you must be an Open Collective Sponsor or Backer of Logseq and therefore log in first. (Để truy cập tính năng mới trước người khác, bạn phải là Sponsor hoặc Backer của Logseq trên Open Collective và đăng nhập trước.)
- The page "{1}" doesn't exist yet. Please create that page first, and then try again. (Trang "{1}" chưa tồn tại. Vui lòng tạo trang đó trước, sau đó thử lại.)
- Plugins (Plugin)

## `:settings.general/`

- Accent color (Màu nhấn)
- Choosing an accent color may override any theme you have selected. (Việc chọn màu nhấn có thể ghi đè giao diện đã chọn.)
- Logseq classic color (Màu cổ điển Logseq)
- Cancel accent color. This is currently in beta stage and mainly used for compatibility with custom themes. (Huỷ màu nhấn. Hiện đang ở giai đoạn beta và chủ yếu để tương thích với giao diện tuỳ chỉnh.)
- What's new? (Có gì mới?)
- Check for updates (Kiểm tra cập nhật)
- Current Revision:  (Phiên bản hiện tại: )
- Current version (Phiên bản hiện tại)
- Custom configuration (Cấu hình tuỳ chỉnh)
- Custom global configuration (Cấu hình toàn cục tuỳ chỉnh)
- Custom theme (Giao diện tuỳ chỉnh)
- Edit config.edn (Sửa config.edn)
- Edit custom.css (Sửa custom.css)
- Edit export.css (Sửa export.css)
- Edit global config.edn (Sửa global config.edn)
- Font (Phông chữ)
- Set as global font family (Đặt làm phông chữ toàn cục)
- Export theme (Xuất giao diện)
- Language (Ngôn ngữ)
- Native title bar (Thanh tiêu đề gốc)
- Enables the native window title bar on Windows and Linux. (Bật thanh tiêu đề cửa sổ gốc trên Windows và Linux.)
- Please refresh the app for this change to take effect (Vui lòng làm mới ứng dụng để áp dụng thay đổi)
- release channel (kênh phát hành)
- Revision: {1} (Phiên bản: {1})
- dark (tối)
- light (sáng)
- system (hệ thống)

## `:settings.sync-server/`

- Sync server URL cleared. Using official Logseq Sync. (Đã xoá URL sync server. Đang dùng Logseq Sync chính thức.)
- Reset to default (Đặt lại về mặc định)
- Sync server URL saved. (Đã lưu URL sync server.)
- Sync Server URL (URL Sync Server)
- Set a custom HTTPS sync server URL for self-hosted sync. Your Logseq authentication tokens will be sent to this server, so only use a trusted URL. Leave empty to use the official Logseq Sync. (Đặt URL HTTPS sync server tuỳ chỉnh để tự lưu trữ. Token xác thực Logseq sẽ được gửi đến server này, vì vậy chỉ dùng URL đáng tin cậy. Để trống để dùng Logseq Sync chính thức.)
- URL must start with https:// or http:// (URL phải bắt đầu bằng https:// hoặc http://)

## `:shell/`

- Input command (Nhập lệnh)

## `:shortcut.category/`

- Basics (Cơ bản)
- Block command editing (Chỉnh sửa lệnh khối)
- Block editing general (Chỉnh sửa khối chung)
- Block selection (press Esc to quit selection) (Chọn khối (nhấn Esc để thoát chọn))
- Formatting (Định dạng)
- Navigation (Điều hướng)
- Others (Khác)
- Plugins (Plugin)
- Toggle (Bật/tắt)

## `:sidebar.left/`

- Favorites (Yêu thích)
- Navigations (Điều hướng)
- Recent (Gần đây)

## `:sidebar.right/`

- Close (Đóng)
- Close all (Đóng tất cả)
- Close others (Đóng các mục khác)
- Collapse (Thu gọn)
- Collapse all (Thu gọn tất cả)
- Collapse others (Thu gọn các mục khác)
- Expand (Mở rộng)
- Expand all (Mở rộng tất cả)
- More (Thêm)
- Open in sidebar (Mở trong thanh bên)
- Open as page (Mở dưới dạng trang)
- Right sidebar resize handler (Tay cầm thay đổi kích thước thanh bên phải)
- Toggle right sidebar (Bật/tắt thanh bên phải)

## `:storage/`

- Invalid data writing to db! (Dữ liệu không hợp lệ ghi vào db!)
- SQLiteDB error: {1} (Lỗi SQLiteDB: {1})
- SQLiteDB import error: {1} (Lỗi nhập SQLiteDB: {1})
- SQLiteDB save error: {1} (Lỗi lưu SQLiteDB: {1})

## `:storage.recycle/`

- Block deleted {1} (Khối đã xoá {1})
- Recycle is empty. (Thùng rác trống.)
- Page deleted {1} (Trang đã xoá {1})
- Recycle is read-only. (Thùng rác chỉ đọc.)
- Restore (Khôi phục)
- Deleted pages and blocks stay here until restored or automatically garbage collected after 30 days. (Trang và khối đã xoá sẽ ở đây cho đến khi được khôi phục hoặc tự động dọn sau 30 ngày.)
- Recycle (Thùng rác)

## `:sync/`

- Downloading assets ({1}) (Đang tải tệp đính kèm ({1}))
- Uploading assets ({1}) (Đang tải lên tệp đính kèm ({1}))
- These server versions conflicted with local edits. Copy the text you want and edit the block manually. (Các phiên bản máy chủ này xung đột với chỉnh sửa cục bộ. Sao chép văn bản bạn muốn và sửa khối thủ công.)
- Sync conflicts (Xung đột đồng bộ)
- Creating remote graph... (Đang tạo đồ thị từ xa...)
- Downloading... (Đang tải xuống...)
- Downloading {1} ... (Đang tải xuống {1} ...)
- Sync graph count exceed limit (Số lượng đồ thị đồng bộ vượt giới hạn)
- Invitation sent! (Đã gửi lời mời!)
- Last synced time: {1} (Lần đồng bộ cuối: {1})
- Mark resolved (Đánh dấu đã giải quyết)
- More debug info (Thông tin gỡ lỗi thêm)
- Offline (Ngoại tuyến)
- Online (Trực tuyến)
- pending local changes (thay đổi cục bộ đang chờ)
- pending server changes (thay đổi máy chủ đang chờ)
- Show sync conflicts (Hiển thị xung đột đồng bộ)
- Something wrong, please try again. (Có lỗi xảy ra, vui lòng thử lại.)
- Start sync (Bắt đầu đồng bộ)
- Sync storage exceed limit (Dung lượng đồng bộ vượt giới hạn)
- Uploading... (Đang tải lên...)
- User doesn't exist yet. (Người dùng chưa tồn tại.)

## `:theme/`

- Logseq Default theme (Giao diện mặc định Logseq)
- Switch to {1} theme (Chuyển sang giao diện {1})

## `:ui/`

- All Done! (Hoàn tất!)
- Apply (Áp dụng)
- Cancel (Huỷ)
- Close (Đóng)
- Configure (Cấu hình)
- Confirm (Xác nhận)
- Copy (Sao chép)
- Copy all (Sao chép tất cả)
- Copy to clipboard (Sao chép vào clipboard)
- Create (Tạo)
- e.g. Next week (ví dụ: Tuần sau)
- Delete (Xoá)
- Deleted (Đã xoá)
- Don't remind me again (Không nhắc tôi lần nữa)
- Empty (Trống)
- Error caught by UI!
 {1} (Lỗi UI!
 {1})
- Export (Xuất)
- False (Sai)
- Fix it! (Sửa ngay!)
- Frequently used (Hay dùng)
- From:  (Từ: )
- Host (Máy chủ)
- image (hình ảnh)
- Label (Nhãn)
- Link (Liên kết)
- Load more (Tải thêm)
- Loading... (Đang tải...)
- Login (Đăng nhập)
- Logout (Đăng xuất)
- OFF (TẮT)
- ON (BẬT)
- Open (Mở)
- Open {1} (Mở {1})
- Port (Cổng)
- Refresh (Làm mới)
- Restart the app to apply this change. Restart now? (Khởi động lại ứng dụng để áp dụng. Khởi động lại ngay?)
- Remove background (Xoá nền)
- Reset (Đặt lại)
- Run (Chạy)
- Save (Lưu)
- Show less (Hiển thị ít hơn)
- Show more (Hiển thị nhiều hơn)
- Skip (Bỏ qua)
- Submit (Gửi)
- To:  (Đến: )
- Toggle theme (Chuyển giao diện)
- True (Đúng)
- Type (Kiểu)
- Untitled (Không tiêu đề)
- Use current time (Dùng thời gian hiện tại)
- Yes (Có)

## `:updater/`

- Checking for updates (Đang kiểm tra cập nhật)
- Downloading update ({1}%) (Đang tải cập nhật ({1}%))
- Restart and install (Khởi động lại và cài đặt)
- Your app is up-to-date 🎉 (Ứng dụng của bạn đã được cập nhật 🎉)
- Update available (Có cập nhật)
- ⚠️ Oops, Something Went Wrong!
Please check out the {1}. (⚠️ Rất tiếc, có lỗi xảy ra!
Vui lòng xem {1}.)
- Update ready to install (Cập nhật sẵn sàng cài đặt)

## `:view/`

- Add new view (Thêm view mới)
- All (Tất cả)
- Export EDN (Xuất EDN)
- Linked references (Tham chiếu đã liên kết)
- New (Mới)
- New property (Thuộc tính mới)
- New view (View mới)
- Rename (Đổi tên)
- Results: (Kết quả:)
- Unlinked references (Tham chiếu chưa liên kết)

## `:view.filter/`

- Custom date (Ngày tuỳ chỉnh)
- Empty (Trống)
- Filter (Bộ lọc)
- from (từ)
- Is Empty (Là Trống)
- Is Not Empty (Không Trống)
- Match (Khớp)
- Match all filters (Khớp tất cả bộ lọc)
- Match any filter (Khớp bất kỳ bộ lọc)
- after (sau)
- before (trước)
- between (giữa)
- date after (ngày sau)
- date before (ngày trước)
- is (là)
- is not (không phải)
- text contains (văn bản chứa)
- text not contains (văn bản không chứa)
- or (hoặc)
- 1 day ago (1 ngày trước)
- 1 month ago (1 tháng trước)
- 1 week ago (1 tuần trước)
- 1 year ago (1 năm trước)
- 3 days ago (3 ngày trước)
- 3 months ago (3 tháng trước)
- to (đến)
- Type to search (Gõ để tìm kiếm)

## `:view.table/`

- Ascending (Tăng dần)
- Columns visibility (Hiển thị cột)
- Delete sort (Xoá sắp xếp)
- Descending (Giảm dần)
- Drag && Drop to reorder (Kéo && Thả để sắp xếp lại)
- Group by (Nhóm theo)
- Journal date (Ngày nhật ký)
- Page created date (Ngày tạo trang)
- Page name (Tên trang)
- Page updated date (Ngày cập nhật trang)
- Live query ({1}) (Truy vấn live ({1}))
- Name (Tên)
- No {1} (Không {1})
- Page (Trang)
- Pages (Các trang)
- Pin (Ghim)
- Row number (Số dòng)
- Select all (Chọn tất cả)
- Select (Chọn)
- Select order (Chọn thứ tự)
- Select row (Chọn dòng)
- Selected: {1} (Đã chọn: {1})
- Sort ascending (Sắp xếp tăng dần)
- Sort descending (Sắp xếp giảm dần)
- Sort groups by (Sắp xếp nhóm theo)
- Sort groups order (Thứ tự sắp xếp nhóm)
- Total refs count (Tổng số tham chiếu)
- Unpin (Bỏ ghim)

## `:window/`

- Close (Đóng)
- Exit full screen (Thoát toàn màn hình)
- Maximize (Phóng to)
- Minimize (Thu nhỏ)
- Restore (Khôi phục)

## `:youtube/`

- Please embed a YouTube video at first, then use this icon.
Remember: You can paste a raw YouTube url as embedded video on mobile. (Vui lòng nhúng video YouTube trước, sau đó dùng biểu tượng này.
Lưu ý: Bạn có thể dán URL YouTube thô để nhúng video trên di động.)
- YouTube player isn't ready yet. (Trình phát YouTube chưa sẵn sàng.)
- YouTube timestamps aren't available on mobile yet. (Dấu thời gian YouTube chưa khả dụng trên di động.)

## `:zotero/`

- Attachments (Tệp đính kèm)
- This is a Zotero imported file, set Zotero data directory to open the file in Logseq. (Đây là tệp đã nhập từ Zotero, hãy đặt thư mục dữ liệu Zotero để mở tệp trong Logseq.)
- This is a Zotero linked file, set Zotero linked attachment base directory to open the file in Logseq. (Đây là tệp được liên kết từ Zotero, hãy đặt thư mục cơ sở của tệp đính kèm Zotero để mở tệp trong Logseq.)
- Notes (Ghi chú)

